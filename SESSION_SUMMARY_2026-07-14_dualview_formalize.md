# Session 总结(2026-07-12 至 07-14):双机位渲染器正式化实验 + 数据体检

> 写给之后接手的人(或未来的自己)。本文不用内部简写:凡是提到"运动预测模型",指的是代号 ② 的
> flow world model(给定机械臂动作,预测物体上跟踪点的未来位置);"渲染器"指代号 ③ 的模型
> (给定第 0 帧图像和某种条件信息,预测未来每一帧图像)。
> 数据集是 can(机器人和人手分别抓取推动一个罐子),每条轨迹有两个机位:俯视相机(cam_high,
> 视野干净)和低角度相机(cam_low,机械臂经常挡住罐子)。
> 配套文档:实验报告 `DUALVIEW_DIT_REPORT.md`、数据体检报告 `CAN_DATA_AUDIT_2026-07-13.md`。

---

## 一、这个 session 要解决什么问题

项目的大方向是跨具身世界模型:用人手演示数据帮助训练机器人的世界模型。此前的探索实验已经初步显示:
给渲染器提供"物体的运动"(把罐子上 48 个跟踪点的位移画成一张运动图)作为条件,比提供"机械臂的动作
数字"效果好。但那个探索实验有四个不够严谨的地方,审稿人都能攻击:

1. 只训练了一次(单随机种子),差距可能是运气;
2. 作为对照的"动作数字"条件太弱(只给了一个手腕点,是稻草人对照);
3. 条件用的是真实的物体运动(上帝视角),没有验证用"预测出来的运动"驱动是否还成立;
4. 没有和 IWS(实验室内部的现成世界模型系统)的原版模型直接比过。

本 session 的目标就是把这个结果做成可以写进论文的严谨版本,并按用户要求补上:和 IWS 原版的直接
对比、人手数据是否有帮助的消融、以及事后的数据管线全面体检。

## 二、参考了哪些论文和现有系统

- **OSCAR(arXiv 2606.04463,北大/密歇根/NVIDIA)**:跨具身、以动作为条件的视频世界模型。它把
  机器人手臂和人手都渲染成同一种二维骨架线画,作为空间条件喂给扩散模型。它的消融里"骨架空间条件"
  最强,"把动作压成向量"最差。我们据此设计了第三种对照条件(见下文 eefsp),并用它解释了一个意外结果。
- **IWS stage2(实验室内部系统)**:它的动力学模型 CMLatentDynamics 用三维卷积做时空骨干,动作向量
  经过一个多层感知机后,对网络每一层做特征缩放(这种注入方式叫 FiLM)。我们的"动作数字"对照臂在代码
  层面复刻了这种注入方式;另外还把它的原版模块整个拿来独立训练作为外部基线。
- **HaMeR(基于 MANO 手部模型的重建方法)**:人手数据的手腕、拇指尖、食指尖三个关键点来自它,
  这次数据体检和真腕修复围绕它的输出展开。
- **Diffusion Forcing(每帧独立噪声等级的扩散训练法)与余弦噪声日程(Nichol & Dhariwal)**:
  训练 IWS 外部基线时,用这套方法复刻了 IWS 原生的"逐帧去噪、自回归滚动"的推理方式。
- 沿用自此前 session 的组件(有各自的判决记录):add 式注入的 DiT 渲染骨干、冻结的 16 通道预训练
  VAE(ostris/vae-kl-f8-d16,渲染器只预测 VAE 的隐变量,再由 VAE 解码器还原像素)、CoTracker
  物体点跟踪。

## 三、做了什么实验(设计)

所有实验共用:同一个 DiT 渲染骨干、同一个冻结 VAE、同样的训练预算(60 轮)、同样的数据划分、
同样的评估(在物体周围裁 64×64 小块算感知相似度 obj-LPIPS,数字越小越像真的;附带检测率防止
"物体消失反而得分"的陷阱)。每种设置训练 3 个随机种子,报均值和标准差;差距超过两倍合并标准差
才允许说"显著"。评估固定用 24 段留出视频,所有实验共享同一份名单。

对比的四种条件方式:

| 名称 | 给渲染器什么 | 怎么给 | 对应文献 |
|---|---|---|---|
| flow(我们的方法) | 罐子上 48 个点的运动 + 夹爪 3 个点 | 画成图,按空间位置加进网络 | 物体运动接口 |
| eefsp | 只有夹爪/手的 3 个点的运动 | 同上(画成图) | 类比 OSCAR 的骨架空间条件 |
| eeffilm | 同样 3 个点,但压成 24 个数字 | 经多层感知机做全层特征缩放 | 复刻 IWS 的动作注入 |
| IWS 原版 | 同样 24 个数字 | IWS 自己的三维卷积网络+扩散训练 | 外部基线 |

另外三条消融:(a) 屏蔽两个机位之间的注意力(参数量不变),测"双机位联合渲染"有没有价值;
(b) 每种条件各做"只用机器人数据"对"机器人+人手数据混训",测人手数据在渲染层是否有帮助;
(c) 端到端:用运动预测模型的输出(而非真实运动)驱动渲染器。

## 四、结果(修正数据泄漏之后的最终数字)

obj-LPIPS,越小越好,"俯视/低角度"两个机位分开报:

| 条件 | 俯视机位 | 低角度机位 |
|---|---|---|
| flow(物体运动,画成图) | 0.256±0.007 | **0.279±0.003** |
| eefsp(只有手,画成图) | **0.228±0.012** | 0.291±0.014 |
| eeffilm(只有手,压成数字,IWS 式注入) | 0.278±0.011 | 0.322±0.021 |
| IWS 原版模型 | 0.324(单种子) | 0.371(单种子) |

1. **主结论成立**:物体运动条件显著好于 IWS 式的动作数字条件(两个机位差距都超过两倍标准差),
   IWS 原版更差。"画成图给"这个条件形式全面好于"压成数字给"。
2. **意外发现(论文必须如实写)**:只画手的 eefsp 在容易的俯视机位反超了 flow——因为罐子基本被
   夹爪带着走,网络看到手就能猜出罐子;这恰好复刻了 OSCAR"骨架空间条件最强"的发现。在遮挡多的
   低角度机位 flow 平均更好,但差距只有 1.2 倍标准差,不能声称显著。因此"物体运动条件的独特价值"
   不靠这张表撑,靠下面第 3 条撑。
3. **端到端成立(这是 flow 独有的能力)**:重新训练的、保证没见过测试视频的运动预测模型,预测点
   平均只偏 2.7/3.7 像素(128×128 画面,验收线 4 像素);用预测驱动渲染,得分 0.270/0.298,离
   "用真实运动驱动"的上限(0.263/0.282)只差一点,仍明显好于动作数字条件(0.285/0.345)。
   eefsp 做不了这件事:它没有可以被预测、可以拿来交互控制的"物体会怎么动"通道。
4. **人手数据在渲染层不帮忙**:三种条件 × 两个机位共六组"加人手 vs 不加"对比,全部无显著差别。
   与先前判断一致:人手数据的价值在运动预测层(此前测得 1.7-2 倍提升),论文引用时注意层级。
5. **双机位联合渲染无增益**:屏蔽机位间注意力后成绩不变。双机位的好处在运动预测层已经兑现
   (两个机位联合预测比单机位准:漂移 2.96→2.57 像素),渲染层各画各的即可。

## 五、数据体检与两处修复(用户指示"修复,同时检查所有其他 bug")

三个独立审计(标注层/打包层/消费层),要求一切结论用 python 在真实数据上验证:

1. **严重问题(已修复):端到端实验有数据泄漏。** 运动预测模型和渲染器的"训练/测试划分"代码写法
   不等价(一个先乱序再过滤,一个先过滤再乱序),导致 24 段测试视频里 22 段落在运动预测模型的训练
   集里。修复:新增划分选项重训了一个干净模型,重考端到端(见上文第 3 条,结论方向未变,预测误差
   数字从 2.1 像素修正为 2.7/3.7 像素)。注意:其他所有表格用的是渲染器自己的干净划分,不受影响。
   **用旧运动预测模型做端到端的任何管线(包括另一个并行 session 的)都可能有同样问题。**
2. **人手"手腕"点缺陷(已修复)**:打包好的训练数据里,人手的"手腕"槽位其实是两个指尖的中点
   (打包脚本从抓握中心+朝向+开度合成三点时丢掉了真手腕),所以人手侧没有"从哪个方向接近物体"的
   信息。上游 parquet 里 HaMeR 的真手腕和手部朝向矩阵都在。修复:生成了补充文件(side-car,原数据
   一字节不动)`wrist_sidecar_{human,robot}.npz`,含不裁剪的真手腕坐标、有效掩码(74% 帧手腕在画面
   内;另一 session 独立测得 26% 出画,两边数字精确吻合,坐标 bit 级一致)和朝向矩阵。使用时必须用
   有效掩码过滤出画帧。
3. 其余各层干净;记录了两个"无实测影响的脆弱点"(NaN 填充约定不统一;条件构造函数里的可见性参数
   是死代码,遮挡信息从未真正进入渲染器——对三种条件一视同仁,不影响对比)。

## 六、过程事故(供以后避坑)

- 磁盘 /scr2 两次写满:一次让两个训练白跑(保存失败),一次让重试任务 27 秒即崩(日志写不进满盘)。
  处置:所有新产物经软链接落到 /scr 盘;`.hf_cache` 和 `outputs/2026-06-11` 已复制到
  /scr/yusenluo/iws_overflow/,原件删不删等用户决定。
- 两个序列化陷阱:IWS 模块含动态生成类,整模型 pickle 会失败,只能存 state_dict;以 `__main__`
  身份训练保存的模型,加载时要把类名注入 `__main__`。
- 集群的 `env` 命令是坏的 shim,脚本里要用 `/usr/bin/env`。

## 七、产物位置

**Google Drive**(文件夹 `iws_evals/2026-07-13_dualviewDiT正式化_flow接口判决/`):
- `01_三臂+IWS对比gif(replay)`:每段测试视频两个机位的五列并排动图——真实视频 | flow | eefsp |
  eeffilm | IWS 原版,可直观看到 eeffilm 列罐子经常糊掉/消失、IWS 列整体更糊。
- `02_端到端pred-flow_4列gif(去泄漏版)`:四列并排——真实视频 | 用真实运动驱动(上限)| 用预测
  运动驱动(端到端)| 动作数字条件,可看到端到端列和上限列肉眼几乎无差。
- `03_report与消融表` 与 `04_勘误后report+数据体检`:报告和表格的快照(04 是勘误后最新版)。

**仓库内**:
- 报告:`DUALVIEW_DIT_REPORT.md`(实验)、`CAN_DATA_AUDIT_2026-07-13.md`(体检)、本文件。
- 代码:`exp_scel_dualview_dit_formal.py`(渲染器三种条件+训练+评估+端到端+出图)、
  `exp_dualview_iws_stage2.py`(IWS 外部基线)、`run_dualview_formal_sweep.sh` +
  `sbatch/dualview_formal_train.sbatch`(集群批量提交)、`agg_dualview_formal.py`(汇总与显著性
  判定)、`augment_clips_wrist.py`(真腕补充文件)、`tests/test_dualview_formal.py`(14 个单元测试)。
- 数据与模型:`outputs/cross_embodiment_wm/dualview_dit_formal/`(每格实验一个目录,实体在
  /scr/yusenluo/iws_overflow/ 经软链接)、干净运动预测模型在
  `outputs/cross_embodiment_wm/dualview_wm_cleansplit/`。

## 八、遗留事项

1. /scr2 上已复制的 `.hf_cache`(4.7G)与 `outputs/2026-06-11`(1.3G),删原件还是删副本,等用户定。
2. IWS 外部基线目前只有一个种子,若审稿需要可再补(一条 sbatch 命令,报告第 6 节有)。
3. 真手腕修好后,另一 session 的"真腕对动作表示没帮助"的负结论被出画帧混杂,值得用有效掩码过滤后
   重跑;人手侧的接近方向(朝向矩阵)是全新的、还没被任何实验用过的信息源。
4. 需转告另一 session:旧运动预测模型做端到端有泄漏风险(见第五节第 1 条)。
