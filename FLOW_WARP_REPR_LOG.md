# 工作日志:flow-warp 渲染 + flow 表示升级(2026-07-14 开工)

> 本文档是这条工作线的**活文档**:给用户随时看进展,也给下个 session 无缝接手。
> 规则:边做边写;每个讨论细节、候选方法、参考论文、实验数字、gif 位置、舍弃理由都记录在此。
> 上一条线(双机位渲染器正式化)的总结见 `SESSION_SUMMARY_2026-07-14_dualview_formalize.md`,
> 实验报告见 `DUALVIEW_DIT_REPORT.md`,数据体检见 `CAN_DATA_AUDIT_2026-07-13.md`。

---

## 0. 一句话目标

治"渲染糊"(用户 2026-07-14 反馈:所有渲染都有些糊):把物体运动从"给渲染器看的条件图"升级为
"直接搬运像素的算子"(flow-warp),同时升级 flow 表示本身(刚体参数化、可见性通道、agent/物体分通道)。
不动已交付的正式化对比结论——那套结论是"哪种条件更好"的相对比较,这条线解决"绝对清晰度"。

## 1. 立项背景与讨论记录(逐条,含用户原话大意)

**2026-07-14 用户反馈一**:"渲染的其实都不好,都有些糊,而且 gif timestep 都太短了"。
- 处置(已完成):gif 从 20 帧加长到 44 帧重出(数据每段 48 帧,起点用 4 帧,44 是上限);
  评估主表保持 20 帧不动,44 帧作为压力测试单独存目录 `e2e_H44/`。
- 44 帧压力测试的数字:运动预测累积误差从 2.71/3.67 像素涨到 3.85/4.93 像素(两个数字分别是
  俯视机位/低角度机位);端到端渲染在俯视机位对"动作数字条件"的优势收窄到 0.005(接近噪声),
  低角度机位仍清晰(0.305 对 0.333)。结论:长时长的瓶颈是运动预测的漂移累积。
- 关于糊的根因分析:渲染器是"确定性回归 VAE 隐变量"的路线,对不确定的未来取平均必然糊;
  物体的印刷纹理(可乐罐图案)网络画不出来,只能画个大概。

**2026-07-14 用户问题二**:"flow-warp 的话原来的点位怎么办?"
- 答(已和用户对齐):48 个点**不抛弃**,它们仍然是运动预测模型的预测目标(整个方法的接口不变);
  warp 只是渲染阶段的新算子——把 48 个点的稀疏位移插值成物体区域逐像素的位移场,把第 0 帧的
  物体像素直接搬到新位置,网络只补接缝、遮挡露出的背景和光照。纹理是搬来的原件,不是画的仿品。

**2026-07-14 用户问题三**:"flow 这个表示还有没有优化空间,不只是 48 个点?"
- 答(候选方法表见第 3 节):有,按性价比排序为刚体参数化、可见性通道接通、agent/物体分通道、
  密集网格 flow、渲染器加辅助预测头。

**2026-07-14 用户拍板**:"可以,今晚做这个,现在开始",并要求维护本活文档。

**2026-07-14 用户指示四(mid-flight)**:"你有没有加入预测的 agent mask 进入模型,没有的话要加,同时也可以
尝试 agent 表示的形式,比如 mask,或者是几个关键点组成的骨架(参考 OSCAR)"。
- 核查结论:双机位正式版渲染器**没有** agent 掩码通道(旧单机位 detmem 线有 4 通道条件含 agent 掩码,
  移植双机位时丢了,条件只剩 3 通道:运动 dx/dy + 物体足迹)。
- **用户澄清(随后)**:指的是 V3 线的专用组件 **MaskGen**(`train_maskgen_v3eef.py`):小网络,
  输入=关节 7 维+眼手三点 6 维,输出=128×128 agent 轮廓掩码;监督=SAM2 机械臂掩码(V3 判决:
  robot_arm_mask 是正确监督);渲染时逐帧从动作合成掩码作第 4 条件通道(`exp_v3_human_helps_pixels.py::
  load_gmask/gmask_imgs`),keyboard 演示的 agent 渲染靠它。双机位移植时整个组件被丢。
  → M1.5 以**复活 MaskGen(can 版,双机位)**为主:掩码监督用暗色连通域提取(暗 agent 浅桌面比 SAM2
  点种稳,必须眼检)→ 训 can 版 MaskGen → 渲染器条件 3 通道变 4 通道(加预测掩码)。
- 设计定案:把"agent 表示"做成与"物体运动表示"正交的第二根消融轴,四种形式:
  (a) 关键点高斯斑 = 已有的 eefsp 臂;(b) **骨架线画**(OSCAR 式,腕→两指尖连线;人手侧用
  wrist_sidecar 的真腕,今天刚修好,正好用上);(c) **合成胶囊掩码**(沿骨架画粗线当 agent 掩码,
  从动作即可生成 = 可预测、可部署,对应用户"预测的 agent mask");(d) **oracle 掩码上界**
  (暗色连通域从真实帧提取,偷看未来帧不可部署,只做上界参考——同"真实运动上界"逻辑;
  选暗色连通域而非 SAM2 依据 [[feedback_mask_quality_must_eyeball]]:暗 agent 浅桌面用它更稳)。
- **用户再澄清(设计定稿)**:"也许可以不是 mask——预测 mask 的位置也不是百分百对,也许骨架版本更容易?"
  → 采纳,顺序翻转:**骨架线画优先**(纯几何画线,零学习零监督,误差只剩动作本身;OSCAR 最强条件同款;
  出画真腕对画线无害——画面内的线段照样正确,不需要 wrist_valid 过滤;线宽用夹爪开度调制,带上 OSCAR 的
  gripper 状态视觉指示)。MaskGen 降级为对照项(学习式掩码 vs 几何骨架的消融行,骨架若显著可不做)。
- **用户追加(定稿)**:骨架可以加上机械臂 arm 的部分,不一定只是三个点(反正是探索不同 agent 表示);
  同时注意不要偷看未来、信息泄漏。
- 现成组件盘点(全部已核实存在):`skeleton_silhouette_qc.py` = 整臂骨架原型(关节角→FK(mujoco/pinocchio)
  →连杆关节点投影→连线加粗,确定性无学习可部署,绕开了 URDF 网格顶点偏移 bug——用关节原点投影);
  `Trossen_Analysis/`(stationary_ai.urdf + FK + 重投影验证);`train_maskgen_caneef.py` 的 can 版
  MaskGen **已训好**(留出 IoU 0.790,qc=outputs/flow_wm/maskgen_caneef/qc.png,监督=暗色∩FK胶囊门控
  ——can 的影子会让纯暗色掩码吞托盘)——但只有俯视机位,低机位需补训;骨架 FK 投影天然双机位。
- **防泄漏规则(写死,所有臂遵守)**:允许作未来帧条件 = 关节角/眼手点/夹爪开度轨迹(动作与本体感知,
  世界模型合法输入)+ 第 0 帧图像 + 端到端时的 ② 预测;禁止 = 任何从未来帧图像提取的量(掩码/轮廓/颜色),
  除非明确标 oracle 上界且不进主表;MaskGen 训练监督来自图像但**推理只吃动作** → 作条件不泄漏。
- 排期:M1(flowwarp)烟测/训练继续;M1.5 = flow 条件 + {无 agent(基线)/**整臂骨架(FK 7连杆链,主)**/
  手部三点骨架(消融整臂 vs 只手)/MaskGen 预测掩码(对照,低机位需补训)} 消融;
  评估加 agent 区感知相似度指标(否则量不出 agent 画得好坏);人手=腕+指尖+前臂方向短线(rot3d)。

## 2. 参考论文与内部先例(为什么相信这条线)

| 来源 | 要点 | 对本线的作用 |
|---|---|---|
| 内部判决 `project_flow_warp_renderer`(exp_scel_flow_warp_render.py) | 在纯色方块上,带掩码的局部 warp ≈ 现有渲染器,没赢;当时结论:"warp 的战场在有纹理的物体上——网络幻觉不出的细节,搬运能保住";全局 warp 会毁掉没有跟踪点的区域(用户当时的警告应验) | 直接的方法来源;can 的可乐罐是强纹理物体,正是它该赢的场景;必须用掩码局部 warp |
| 内部组件 `footprint_warp.py` | SAM2 分割物体掩码(跟踪点作正提示、夹爪点作负提示)+ Umeyama 相似变换拟合搬运,物体无关,覆盖率 0.41→0.78 | 刚体拟合+掩码搬运的现成实现,直接复用 |
| AMPLIFY(arXiv 2502.20391) | 密集网格 flow(20×20 全画面跟踪)+ 压成离散 token;每帧重铺跟踪点防漂移 | 候选方法 4(密集网格)的来源;其"稀疏点→密集场"解码器思路是插值方案之一 |
| DexWM(调研文档 FLOW_REPR_SURVEY_2026-07-04.md) | agent 的运动和物体的运动分开通道 | 候选方法 3 的来源 |
| MaskWAM(arXiv 2606.13515) | 把运动只当输入条件、不要求网络预测输出,性能 84.9%→21.6%;条件必须配预测监督 | 候选方法 5(辅助预测头)的证据 |
| 内部体检 `CAN_DATA_AUDIT_2026-07-13.md` | 渲染器条件构造里的可见性参数是死代码,遮挡信息从未进入渲染器 | 候选方法 2(接通可见性)的依据 |
| 内部判决 `project_amplify_grid_probe` | 密集网格 flow 的人手/机器人可分性检测未过(0.82),但下游人手数据增益仍 +39-47% | 说明候选方法 4 没有被真正判死,可分性检测不是硬门 |

## 2.1 EgoWAM / LaST-HD 精读(2026-07-15)

**EgoWAM**(Georgia Tech,`gatech-rl2.github.io/egowam.github.io`,官网+PDF 全文已直接读过,以下为一手信息):

- 是什么:一个"世界-动作模型"(World Action Model)框架。在标准的具身预训练 Transformer(HPT)
  骨干上,除了动作头,额外挂一个**可替换的世界模型头**,用"预测未来状态"作动作监督之外的第二条
  监督通道;推理时世界模型头直接摘掉,只用动作头解码,不增加推理延迟(论文原话:训练时做
  representation shaping,而不是测试时想象未来)。
- 具身接口设计:**不用骨架**,而是给每种具身一个独立的浅层编码器("stem":ego 视觉 stem、腕部
  视觉 stem、本体感受 stem),各自的 token 喂进同一个共享 Transformer trunk;human/robot 能不能
  对齐,靠的是共享 trunk + 世界模型辅助 loss 把两域隐变量拉到一起(论文 UMAP 图:纯行为克隆
  下 human/robot 隐变量各自成团,加了世界模型头后两团合并)。这和 OSCAR 是两条不同路线——
  OSCAR 在**输入端**强行统一表示(同构骨架线画),EgoWAM 在**输出端(监督信号)**统一,输入端
  各具身各自编码互不同构。
- 世界模型目标(world target)的选择是全文核心贡献,比较三种:像素 VAE 重建(迁移最弱,论文
  说重建目标会把 trunk 推去编码"具身专属外观",挤占跨具身共享结构);DINO 特征(语义抽象,
  分布外物体/场景泛化最强,+最多 4×);相机运动去除后的 3D 稀疏 flow(域内空间精度提升最大,
  +20-30%,论文认为它同时满足三条设计准则:外观抽象、跨具身一致——监督"物理后果"而非
  "外观"、以及去除自我运动干扰)。
- 去自我运动(ego-motion)手段:human 数据来自头戴 Aria 眼镜,摄像头随头动会把"转头"和
  "物体动"混进同一个 2D flow;EgoWAM 先用 VIO 相机位姿把 3D 点位移转到静止世界系,算出
  "物体相对世界"的真实位移,再投影回 t 时刻相机系当监督目标——静止背景的 flow 因此趋近于零,
  只留下真正被操纵物体的位移。
- 训练配方:联合 loss = 动作 loss + λ·世界模型 loss(λ=1),机器人 300-360 条示教 + 两档人类
  数据(域内对齐 1:1、野外不对齐约 10:1)。专门做了"故意让人类数据动作不对齐"的消融:行为
  克隆在不对齐数据下直接崩到低于纯机器人基线(成功率 40%→20%),而 3D-flow 世界模型头保持
  稳健(75% 不掉)。
- 局限(论文自述):增益停留在"同一动作原语内泛化",学不到人类数据里机器人没见过的新动作;
  多任务联合训练未做;三种目标里哪个"最优"仍是开放问题,论文定位为"提出研究这个轴"而非终局答案。

**对我们的启发**(逐条映射候选方法表):
- 支持候选 #5/#6(密集 flow、辅助预测头)的方向:3D-flow 作"辅助监督目标"依然是效果最好的一档,
  且辅助头训完可摘、不增加推理成本——这给候选 #6"渲染器加辅助预测头"提供直接正面证据:如果
  给 ③ 加辅助头,目标该是 flow/动态而非像素重建,论文明确指出像素重建目标会挤占跨具身共享的
  结构信号,这和我们 M1 flowwarp 判死("像素搬运路线不赢")是同一机制的独立佐证,不是巧合。
- 不直接支持也不反驳骨架条件,因为不在同一层面:EgoWAM 整篇没有用骨架/关键点表示,它解决的是
  "监督信号该是什么"而非"输入条件该是什么",不能拿它当我们骨架线画条件(M1.5,渲染器 ③ 输入端)
  的证据或反证——但它"外观抽象优于像素重建"的设计准则,和我们骨架线画(几何抽象、非外观)背后
  的直觉一致,算间接背书。
- **新启发,已加入候选表(见下 #9)**:human 数据若来自会动的头戴相机(比如 phantom aytsai 那条
  ego 数据),flow 标注在拿去做 ② 目标或 ③ 条件前应该先做"相机自运动去除"(相机位姿把 3D 位移
  搬到世界系再投影回来),否则"转头"产生的 flow 会污染"物体真的在动"这个信号;can_dual 当前是
  静态桌面相机,预期不受影响,但 aytsai 线要留意。
- 呼应我们的 α 汇率结论(`COMBINE_ACTION_LOG.md` §5):EgoWAM"行为克隆对未对齐人类数据脆弱、
  世界模型辅助 loss 稳健"这个消融,从另一个任务/架构独立复现了我们"human 数据要靠机制(而非纯
  action co-train)才稳"的判断,支持继续把 align_wm/骨架这类机制路线当 paper 主线,而非退回纯
  action co-train。

**LaST-HD**(2606.23685;以下继承自另一 session 的精读笔记 `COMBINE_ACTION_LOG.md` §0/§4.3/§4.6,
本次未重新抓取原文,只做二次核查与对我们线的映射):
- 机制:独立训练一个跨具身(human+robot 混训、无需配对)的动作条件世界模型,冻结后产生"未来
  latent"目标,用 cosine 相似度监督主模型的 latent reasoning;loss = L_act + λ·L_latent。
- 核心消融(继承笔记,Fig 3b):目标必须同时满足"动作条件"+"有预测性"+"被真实未来 grounding"
  三者,四档从高到低是动作条件世界模型 73% > 视觉 SigLIP 特征 66% > 无动作条件世界模型 63% >
  无 latent 监督 60%——纯视觉特征不够,必须是"动作驱动、预测物理后果"的特征。
- 对我们 ② 机制 sweep(`COMBINE_ACTION_LOG.md` §4.3)是直接方法来源:`align_wm`(冻结 grounded
  教师 + cosine 对齐)是 LaST-HD 的忠实复现,在 v3 和 dual 两个数据集上都显著压制其它机制(a1
  加性残差、warm 两阶段、align 自蒸馏),且从不伤害精度——"目标端对齐 > 输入端同构化"的实证
  结论已经在另一 session 落地。
- **迁移到我们线的新意义**(本次精读补充,非重复其工作):① 对 ③ 渲染器的启发——如果按候选 #6
  接辅助预测头,目标该长什么样,LaST-HD 和 EgoWAM 给出同一个答案(动作驱动 + 被 grounding 的
  动态特征,不是外观特征),两篇独立收敛加强了这条设计原则的可信度;② 对我们刚立项的"方向 1:
  同构骨架作 ② 动作表示"实验(2026-07-15 新开工)是一个预期校准——LaST-HD 的 sweep 已证明
  "让输入同构"(a1/skel 这类输入端手段)会拿精度换 Δ(有代价),只有"目标端对齐"(align_wm)是
  免费的;所以骨架作 ② 输入的新实验大概率会重复出"human-helps Δ 更大但 rh 精度更差"的 Pareto
  形状(和 velocity_action 线、a1 的判决同构),不太可能是免费午餐,实验设计上应同时报 rh 和 Δ,
  不要只看 Δ 就下结论。

**诚实标注(哪些核实过、哪些继承)**:
- EgoWAM 的全部要点(架构、三种世界目标数字、消融数字、去自我运动方法)已通过官网 WebFetch +
  PDF 全文直接核实,一手信息。
- LaST-HD 部分完全继承自另一 session 的 `COMBINE_ACTION_LOG.md` 笔记,本次未重新抓取 2606.23685
  原文,任何数字如与另一 session 笔记不一致以他们的原始记录为准;"迁移到我们线的新意义"两条是
  本次基于我们这条线的上下文做的映射推理,不是原文陈述。
- `COMBINE_ACTION_LOG.md` §4.6 显示另一 session 已经做过一次 EgoWAM 启发的直接消融("stems"
  机制:独立动作编码器 + 共享 trunk + 隐式对齐),判决为"稀缺区不够、充足区打平"——这是对
  EgoWAM"共享 trunk 隐式对齐"设计在我们数据规模下适用边界的第一手实证,精读之前已存在,列在
  此处防止重复劳动。

## 3. 候选方法表(含采纳状态,随进展更新)

| # | 候选 | 内容 | 状态 |
|---|---|---|---|
| 1 | **flow-warp 渲染**(里程碑一) | SAM2 物体掩码 + 48 点拟合刚体/相似变换 + 搬运第 0 帧物体像素,网络补洞 | **进行中(今晚)** |
| 2 | 刚体参数化的 flow 表示 | 运动预测模型直接预测每视角一个变换(4-6 自由度)而非 48×2 个数;罐子是刚体,自由度过剩易散架 | 里程碑二候选,未开始 |
| 3 | 可见性通道接通 | 遮挡信息(哪些点被挡)真正喂进渲染器;低角度机位遮挡最多 | 里程碑二候选,未开始 |
| 4 | agent/物体分通道 | 夹爪点和罐子点分成两个条件通道 | 里程碑二候选,未开始 |
| 5 | 密集网格 flow | 20×20 全画面网格跟踪替代物体 48 点 | 延伸项,未开始 |
| 6 | 渲染器辅助预测头 | 渲染器除了吃运动条件,还要输出未来运动/掩码的预测 | 延伸项,未开始 |
| 7 | 预测 agent 掩码通道(MaskGen 复活) | 动作→掩码小网络(can 版双机位;监督=暗色连通域+眼检) | M1.5 对照项(骨架显著则可不做) |
| 8 | **agent 骨架线画**(用户指示,OSCAR 式) | **整臂**:FK 7 连杆链投影连线(skeleton_silhouette_qc 原型现成),线宽=夹爪开度;另设手部三点版消融;人手=sidecar 真腕+指尖+前臂方向 | **M1.5 主项(优先于 MaskGen)** |
| 9 | flow 目标相机自运动去除(EgoWAM 启发) | 有相机位姿的场景下,先用位姿把 3D 点位移转到静止世界系算真实物体位移,再投影回当前相机系,把"转头/相机动"从 flow 目标里减掉,再喂给 ② 或 ③;can_dual 静态桌面相机预期不受影响,主要针对 aytsai 头戴 ego 数据 | 提案,未开始 |

## 4. 实验记录(逐条追加,含设置/数字/gif 位置/结论)

**[进行中] M1 flowwarp seed0**(SLURM job 53330,commit aeb464e,审查通过):
- 实现:每机位对第 0 帧做掩码局部像素搬运(48 点高斯 splat 反向 warp 场)→ VAE 编码成 z_warp 作为
  第三组 token 进 DiT → 输出 = z_warp + 残差(网络只补洞不重画)。条件图与 flow 臂完全一致。
- 烟测:2 轮 loss 0.82→0.57;warp 开销低于测量分辨率,预计全量 3-4.5 小时(flow 臂 3 小时)。
- **设计选择(记录,防以后当 surprise)**:M1 不做"旧位置擦除"——搬运不抹掉罐子在第 0 帧位置的残影
  (ghost),交给残差网络盖;若评估 gif 里 ghost 成为主要伪影,V3 先例的 edit-mask(新旧足迹并集
  =允许编辑区)是第一个升级项。
- 挂账:flowwarp 的 crossview=0 泄漏单测缺失(reviewer 手工验证过正确;当前不跑 crossview=0,
  任何此类运行前须补测试)。

**[已完成] M1.5 前置:骨架 sidecar**(commit 7d22613,复审通过):
- `skel_sidecar_robot.npz`:FK 整臂 9 点链双机位(2700,48,9,2),覆盖 100%,夹爪端点距眼手指尖 2.7-2.8px,
  link_6 对 eef 基点 0.02px(字节级一致);`segments` 连接关系 + `grip` 随附。
- `skel_sidecar_human.npz`:腕+两指尖+前臂方向短线(1800,24,4,2);前臂方向弃用旋转矩阵轴(逐帧不稳,
  有诊断图),改用 21 点手模的"腕−掌指关节质心"方向,顺带补齐了低机位的腕/前臂(仍零泄漏——全部来自
  标注层动作侧列,复审逐行核实无任何未来帧图像依赖)。
- QC 眼检(controller 亲验):机械臂链夹爪端骑在罐子两侧贴合;人手三线解剖学正确。
  图:outputs/cross_embodiment_wm/skel_sidecar_qc/。
- **消费端须知(复审量化)**:夹爪闭合时 FK 手指端点与真实指尖偏差可达 ~19px(FK 名义张开姿态,
  与 grip 反相关 r=-0.98);线宽随开度调制部分补偿,依赖端点精确位置的用法要小心此尾部。

**[已完成,单种子] M1.5 flowskel seed0**(job 53332,209 分钟,60 轮):
- 设置:flow 臂 3 通道条件 + 第 4 通道 = 整臂 FK 骨架线画(9 点链,线宽随夹爪开度 2-5px;人手=腕+指尖
  +前臂短线),其余与 flow 臂协议完全一致。
- **结果:双机位全面刷新纪录**——物体区感知相似度 0.194(俯视)/0.229(低角度),对比 flow 臂
  0.256/0.279、eefsp 0.228/0.291;PSNR 24.90/23.69 全场最高;agent 区感知相似度 0.214/0.251
  (基线的 agent 数字等补测 job 53335 出来后对比)。
- 判读(初步,单种子):改善幅度 ~0.06 远大于此前测得的种子波动(~0.01),大概率真提升;
  用户"agent 表示重要、骨架形式好"的直觉命中。待办:补 seed 1/2 确认显著性;
  出对比 gif 眼检 agent 画质;更新正式化 report 时需说明这是新增臂(不动已发布结论的叙述)。

**[已完成,单种子] M1.5 flowskel3 seed0(只手骨架消融)**(job 53333,181 分钟):
- 物体区 0.199/0.240,agent 区 0.219/0.261——与整臂版(0.194/0.229,0.214/0.251)几乎打平,
  整臂全指标略优但差距(0.005-0.011)在单种子噪声内。
- 判读:增益主体来自"骨架线画"这一表示形式(线段几何 vs 高斯斑/纯 flow),整臂链的边际贡献小;
  与 OSCAR 的发现方向一致(它的整臂骨架含全部连杆,我们的数据里臂基座大多在裁剪框外,可能压低了整臂的
  边际价值)。paper 叙述:骨架通道是主效应,整臂 vs 只手作次级消融行。

**[已完成,单种子] M1 flowwarp seed0 判决**(job 53330,256 分钟):
- 物体区 0.271/0.282 vs flow 基线 0.263/0.282——数字平局偏负;眼检(compare_newarms gif):纹理在
  但位置略偏、旧位置有轻微 ghost,不优于 flowskel。
- **判决:搁置**(与 v3 方块判决一致:掩码局部搬运不赢确定性重画;"纹理战场"假说未兑现——骨架通道
  用更便宜的方式解决了大部分清晰度问题)。可能复活条件:做了旧位置擦除(edit-mask)且骨架臂在某场景
  仍不够锐时。
- flow 基线 agent 区补测(job 53335):0.296/0.311,物体区复测与发布版完全一致(复测口径可信)。

**终版判决(3 种子齐,2026-07-15)**:flowskel 物体区 0.1925±0.0014 / 0.2318±0.0039,
对 flow 基线差距 0.063/0.047 = 显著性门槛(2σ)的 6-7 倍,PSNR 24.95/23.75 全场最高且种子方差全场最小
(±0.04!)——骨架通道不但更准还让训练更稳。**正式采纳为默认渲染配置。**

**当晚初判(单种子,已被上行确认)**:flowskel(骨架线画通道)全面采纳——物体区 0.194/0.229
(基线 0.263/0.282),agent 区 0.214/0.251(基线 0.296/0.311);整臂 vs 只手近平(整臂略优)。
对比 gif:outputs/cross_embodiment_wm/dualview_dit_formal/compare_newarms/gifs/(Drive 见 07 文件夹)。

**[已完成] 44 帧长时长压测(用户要求 gif 加长后补测,MODE=eval HORIZON=44,seed0)**:
| 臂 | 物体区 H44(vs H20) | agent 区 H44 |
|---|---|---|
| flow | 0.265/0.284(0.263/0.282,退化) | 0.305/0.323 |
| **flowskel** | **0.193/0.227(0.194/0.229,零退化!)** | 0.214/0.252 |
| flowskel3 | 0.198/0.234 | 0.220/0.260 |
| flowwarp | 0.285/0.294(0.271/0.282,退化最多) | 0.329/0.343 |
- **额外发现:骨架通道让长程渲染漂移基本消失**(44 帧与 20 帧持平),对基线差距拉大到 0.072/0.057;
  解释:agent 位置被确定性骨架锚定,自回归误差不再通过 agent 区域累积。眼检(44 帧最后一帧)确认。
- 44 帧 gif:outputs/cross_embodiment_wm/dualview_dit_formal/compare_newarms/gifs/
  (Drive:iws_evals/2026-07-15_flow表示升级_骨架与warp/08_新臂对比gif_44帧长版)。
- 骨架条件通道可视化(用户过目用,含闭合/低机位/人手边界情形):
  outputs/cross_embodiment_wm/skel_channel_viz/skel_channel_cases.png;闭合夹爪时叉尖略开(FK 名义姿态,
  已知 ~19px 尾部),待用户裁决是否用开度收拢端点。

**[已完成] 骨架臂端到端(部署配置判决,2026-07-15,commit 0482975)**:
部署配置 = 运动预测模型的输出驱动 3 个 flow 通道 + **动作直接画骨架通道**(骨架不需要预测,零泄漏)。
| 列(20 帧 / 44 帧) | 物体区 v0/v1 | agent 区 v0/v1 |
|---|---|---|
| 基线天花板(flow,真实运动) | 0.263/0.282 · 0.265/0.284 | 0.296/0.311 · 0.305/0.323 |
| 基线端到端 | 0.270/0.298 · 0.277/0.305 | 0.306/0.323 · 0.320/0.340 |
| naive(eef-FiLM) | 0.285/0.345 · 0.282/0.333 | 0.301/0.356 · 0.304/0.353 |
| 骨架天花板 | 0.194/0.229 · 0.193/0.227 | 0.214/0.251 · 0.214/0.252 |
| **骨架端到端(部署)** | **0.202/0.246 · 0.209/0.253** | **0.223/0.267 · 0.231/0.275** |
- **判决:骨架臂端到端全面成立**——部署配置好于其他所有臂的"作弊天花板";距自身天花板 0.008-0.026;
  44 帧长时长仍稳。② ADE 2.71/3.67(20帧)、3.85/4.93(44帧)。
- Drive:11/12(6 列 gif 20/44 帧)、13(指标表)@ iws_evals/2026-07-15_flow表示升级_骨架与warp/。

**[2026-07-15 用户三连指令与新发现]**
- 用户拍板:跑方向 1(同构骨架作 ② 动作表示,打"动作端 disjoint"瓶颈)+ 方向 3(latent 残差混训移植 can_dual);
  另观察:物体 48 点"不是 can 形状,是一小坨"+ 渲染仍糊且有闪烁噪点。
- **物体点覆盖问题坐实**(QC 图 Drive 16 号):俯视凸包最小 61-74px²(罐顶 ~600+),点挤在红色罐口边缘;
  根因=生成时 HSV 红色撒点(与 footprint 老问题同源);低角度 287-407px² 稍好。**连带:物体区评估裁剪窗
  以红边小坨为中心,非罐子中心。** 处置:先做 30 clip 重撒原型(SAM2 掩码内均匀撒点+CoTracker 重跟),
  量化覆盖改善+全量重生成成本,再和用户/另一 session 协调是否出 tracks-v2(会改变评估口径,老数字不可比)。
- 糊+闪烁:LPIPS 权重 ×2/×4、时长 ×2 三个杠杆已提交(flowskel 上);闪烁噪点候选=时间一致性损失
  (detmem 线有先例),看杠杆结果后决定。
- 三个后台实验同时开工:②骨架动作表示(dummy5 vs skel × robot-only vs +human @ NROB=100)、
  latent 残差 can_dual 移植(residual vs direct × mix)、重撒原型。

**[已完成] 物体点重撒原型**(commit 9466753,30 clip 双机位):
- 覆盖率:俯视 0.33→**0.82**,低角度 0.50→0.83;0 失败 0 漂移;SAM2 掩码选择须用原生分数(footprint_warp
  的"防爆炸"启发式在这里恰好有害——旧凸包本身就小 5-10 倍,罚大掩码等于保住 bug)。
- 全量重生成成本:~3 GPU 小时(128 分辨率)/ 最多 6-10(若按原始 224 跟踪分辨率),可并行到 1.5 小时内。
- **待用户拍板**:tracks-v2 会让已发表的物体区指标不可比(评估裁剪窗随点位变),且数据集与另一 session
  共享;收益=flow 通道/足迹/评估中心全部修正。Drive 17 号图。


**[2026-07-15/16 夜 大批量迭代结果]**

**A. 糊杠杆(flowskel 上扫 LPIPS 权重与训练时长)** — ★赢家=延长训练:
| 配置 | 物体区 v0/v1 | PSNR |
|---|---|---|
| flowskel base(LAM=1,60轮) | 0.194/0.229 | 24.9/23.7 |
| LPIPS 权重×2 | 0.205/0.249(变差) | 24.4/23.2 |
| LPIPS 权重×4 | 0.228/0.266(更差) | 23.5/22.5 |
| **训练×2(120轮)** | **0.157/0.198(大幅变清)** | **25.6/24.4** |
判读:加大感知损失权重反而糊(它牺牲像素保真去凑感知);**便宜有效的治糊杠杆是延长训练**(120 轮把
物体区从 0.194 压到 0.157,PSNR +0.7)。下一步:骨架臂 +120 轮做默认;闪烁噪点若仍在,再上时间一致性损失。

**B. flowskelv2 同构骨架臂** — 略逊于 v1,符合"③ 是 embodiment-specific"的预判:
物体区 0.200/0.238 vs v1 flowskel 0.194/0.229。同构化(拓扑对齐+几何开度)对 ③ 渲染没有正收益
(渲染外观本就不跨具身共享);但 v2 的价值在别处兑现了——见 D(它是 ② 骨架动作实验的基础)。

**C. latent 残差混训(Task#21 移植 can_dual)** — ★复现成功,机制坐实:
| 臂 | robot-only | robot+human | human 增益 |
|---|---|---|---|
| **残差(Δz)** | 0.260 | **0.174** | **−33%(帮)** |
| 直接(绝对 z) | 0.193 | 0.198 | +3%(不帮/微伤) |
(held-out robot latent-MSE,越小越好;全量参照 残差 0.145 / 直接 0.159)
判读:**残差预测下 human 数据帮 robot(−33%),直接预测下不帮**——精确复现旧判决"起点跨域不共享、
动力学 Δz 跨域共享"。这是 flow 之外的第二条独立"human 帮得上"证据。注意 Pareto:残差 robot-only 更差
(0.260 vs 0.193),但有 human 数据后反超——同域表示以牺牲单域精度换 human 迁移(与 EgoWAM/LaST-HD 一致)。

**D. ②骨架动作表示 human-helps(drift px,越小越好,机器人稀缺 100 条)** — ★今晚最强结果:
| 动作表示 | robot-only | robot+human | human 增益 |
|---|---|---|---|
| dummy5(旧,绝对星座) | 9.45/9.27 | 6.36/6.90 | −33%/−26% |
| **同构骨架(v2)** | 11.08/10.60 | **5.69/5.92** | **−49%/−44%** |
(全量参照 骨架 rh 3.43/4.17)
判读:**同构骨架动作把 human-helps 从 ~30% 放大到 ~46%,且 skel+human(5.69/5.92)绝对精度也最好**
——正面命中用户命题"让 human 帮更多"。诊断的动作端瓶颈(输入 disjoint)被同构骨架打中:两具身第一次
用同一几何语言表达动作。同样是 Pareto(骨架 robot-only 更差 11 vs 9.45),但有 human 就反超——
与 C、与 LaST-HD 预测完全一致。★注意归因:骨架臂人手侧用了真腕(修复过),dummy5 人手腕是退化中点,
增益里有一部分来自数据修复而非纯表示——需要一个"dummy5+真腕"对照才能完全隔离,记为待办。


**[★2026-07-16 重要修正:pred flow 叠加暴露长时长塌缩]**
用户质疑"22 号 gif 里 can 没跟着动、而且没有 pred flow"——两条都对,gif 的 flow 行原本所有列都画 GT,
看不出 ② 预测好坏。修复后每列叠该臂预测点(红)over 真值(绿),结果暴露真问题:
- **第 24 步**:四臂红点都跟着绿点,差异清晰(骨架+人手红点几乎压在绿点上,只机器人偏出)——健康。
- **第 47 步**:四臂红点**全部塌到画面底部同一位置**,完全脱离真值——② 崩了。
- 原因:这些 ② 是 20 步 rollout 训练的稀缺模型(100 条机器人),外推 44 步远超训练范围,退化到同一吸引子。
- **诚实修正**:H=44 的 obj-LPIPS(骨架+人手 0.217)有相当部分是**骨架通道(用正确动作)撑着的**,
  不全是 flow 预测的功劳;flow 的真实贡献要看 H=20(② 在训练范围内)。已补跑 H=20 版对照(job 53535)。
- 教训:长时长 gif 必须叠 pred flow,否则"渲染看起来还行"会掩盖 ② 已经崩了这个事实。

**[★2026-07-16 核心交付] ② human-helps 穿过 flowskel 好渲染器(用户要求:真渲染 44 步,不是抽象 drift/糊解码器)**
端到端:四个 ② 模型(稀缺 100 条机器人)rollout 44 步 pred flow → 同一个 flowskel ③(ep120 最清)渲染,
obj/agent-LPIPS(8 序列,越小越好):
| ② 动作 | 只机器人→③ obj | 加人手→③ obj | human 增益 |
|---|---|---|---|
| dummy5 | 0.306/0.353 | 0.246/0.302 | −20%/−14% |
| **同构骨架** | 0.308/0.362 | **0.217/0.270** | **−30%/−25%** |
判读:**② 的 human-helps 真的让最终渲染画面变好**(不只是抽象 drift px)——骨架+人手渲染 obj 0.217,
明显优于只机器人 0.308 和 dummy5+人手 0.246;同构骨架在最终渲染层也放大 human 增益。44 步 gif 眼检:
骨架+人手列罐子位置最贴真值,只机器人列漂走。gif=Drive 22 号(5 列 GT|dummy5-r|dummy5-rh|skel-r|skel-rh)。
注:用旧物体点(tracks-v2 park 成 TODO,当前所有实验用旧点)。

## 5. 舍弃项与原因
- **flowwarp(掩码像素搬运)**:数字平局(0.271 vs 0.263)+ 眼检不优 + ghost 伪影;搁置,复活条件见上。

- 人手前臂方向的"旋转矩阵轴投影"方案:逐帧视觉不稳定(诊断图 forearm_axis_diag.png),
  改用 21 点手模方向,弃。

## 6. 给下个 session 的接手要点

- 环境:conda iws;GPU 任务一律 sbatch(模板 `sbatch/dualview_formal_train.sbatch`,SCRIPT+env 经
  --export 传);集群 bare `env` 是坏的,用 `/usr/bin/env`;磁盘写 /scr(经 symlink),别写满 /scr2。
- 数据:`outputs/flow_render_dataset_can_dual/`;真腕补充文件 `wrist_sidecar_{human,robot}.npz`
  (用 wrist_valid 过滤);评估固定 seq 名单 `eval_seqs_n24.json`。
- 基线:正式化的 flow 条件渲染器 ckpt 在 `outputs/cross_embodiment_wm/dualview_dit_formal/flow_cv1_s0/`
  (以及 s1/s2);干净的运动预测模型在 `dualview_wm_cleansplit/wm_dual.pt`。
- 评估口径:`exp_scel_dualview_dit_formal.obj_lpips_audit`(物体区 64×64 感知相似度+检测率),
  20 帧主表、44 帧压力测试;gif 用 `viz_combined.save_combined_gif` 协议,传 Drive
  `upload_evals_gdrive.sh`。

---

## 2026-07-19 ② 长程 rollout "预测停住" 的排查(含两次我自己的错误判断)

### 起因
用户看 44 帧端到端 gif,指出两件事:(1) "我记得 v3 cube 44 帧的挺好的";
(2) "我看你十几帧,flow 预测就'停止'了"。

### 错误判断 #1:"超出训练 horizon"(已推翻)
我最初解释成"模型只用 20 步训练,44 步超出范围"。用户直接否掉:
**"你不是 autoregressive 的吗,为什么一定要和训练时候一致"**。用户是对的,自回归模型本就该外推,
滚不动是缺陷而不是借口。而且我的事实也错了 —— 训练用的是 `R_SS=32` 的 scheduled sampling,
不是 20 步;F=20 是预测头的未来跨度,不是训练 rollout 长度。

### 错误判断 #2:"动作窗口填充"(真实存在,但不是主因)
第二次诊断发现 `rollout_dual` 每步需要 24 帧动作窗口 `efA[:, h:h+K+F]`(K=4,F=20),
而 can dual-view 的 clip 只有 48 帧:

| rollout 步 | 需要动作帧 | 真实 | 填充(重复末帧) |
|---|---|---|---|
| 0–24 | 0–48 | 24 | 0 |
| 30 | 30–54 | 18 | 6 |
| 44 | 44–68 | 4 | **20** |

填充代码 `wa[:, -1:].repeat(1, pad, 1, 1)` 等于告诉模型"机械臂从此静止"。
这确实是评估代码缺陷,**但它从第 25 步才开始,解释不了用户看到的"十几帧就停"**。

### v3 为什么好:数据长度不同(已核实)
- `flow_render_dataset_v3_long/clips_robot.npz` 也是 **48 帧**(和 can 一样);
- 但 v3 的长 rollout 评估用的是**单独构造的 64 帧序列**
  `outputs/flow_wm/rollout_eval/seqs.npz`,`tracks (36, 64, 48, 2)`,H=40 → 40+24=64 恰好用满,**零填充**。
- 结论:v3 当年为长 rollout 专门备了够长的评估序列;can 这次直接拿 48 帧训练 clip 硬跑 44 步。
  can 要做等价的 44 步评估,必须先抽 ≥68 帧的评估序列。

### 真因实测:速度系统性欠预测(从第 0 步就存在)
逐步测预测点质心的帧间位移(16 条运动最大的序列,cam_high):

```
step   GT      dummy5_r   dummy5_rh   skel_r   skel_rh   (px/step)
   0  2.775      1.492      2.905      1.091     2.733
  10  4.459      1.565      2.597      1.213     3.124
  20  4.368      1.575      2.806      1.128     2.459
  42  4.032      0.984      1.540      0.519     2.264
```

| | 平均速度 | 占 GT | 44 步累积位移 |
|---|---|---|---|
| GT | 3.52 px/step | 100% | ~168 px |
| skel robot-only | 1.0 | **26%** | ~43 px |
| dummy5 robot-only | 1.35 | 35% | ~58 px |
| skel +human | 2.9 | **74%** | ~125 px |

预测点**一直在动,速度没归零**,但幅度只有真值 1/4~1/3。前十几帧位移差看不出来,
之后被真值远远甩开 → 视觉上就是"停在原地"。用户的观察比我的两次诊断都准。

### 候选修法 #1:换速度解码方式 —— **否定**
`amplify_fsq.py:92` `expected_vel = softmax(logits) @ vgrid`(15×15=225 个速度 bin 加权平均)。
假设:加权平均把速度抹平了(回归均值)。换 argmax / 温度锐化 / 采样 对照(同一批已训模型,只换解码):

```
arm             decode      速度px/s   占GT   drift@20  drift@43
skel_r_n100     expected      0.93    26%     31.32     20.06
skel_r_n100     temp0.5       0.87    25%     29.95     30.68
skel_r_n100     temp0.3       0.77    22%     31.89     41.24
skel_r_n100     argmax        0.52    15%     41.96     54.69
skel_r_n100     sample        1.08    31%     35.48     33.83
skel_rh_n100    expected      2.46    70%     15.67     17.35
skel_rh_n100    argmax        2.36    67%     23.36     22.33
dummy5_r_n100   expected      1.35    38%     25.02     17.91
dummy5_r_n100   argmax        0.58    16%     42.23     41.75
dummy5_rh_n100  expected      2.36    67%     15.92     17.77
```

**argmax 比期望值更慢(15% vs 26%)、drift 更差(20→55)。** 说明速度分布的**峰本身就在零附近**,
长尾才在快速方向,期望值是靠尾巴才把速度拉到 26%。
→ 回归均值假设错误;解码不是杠杆;`expected` 已是四种里最好的。**舍弃这条修法。**

### 否定结果带出的真发现
+human 的模型**对解码方式几乎免疫**(argmax 67% vs expected 70%,drift 差 7px),
robot-only 则对解码极度敏感(argmax 15% vs expected 26%,drift 差 34px)。
→ **病根在 robot-only 模型本身,它没学会"罐子会动";human 数据把分布的峰修正了。**

### 下一步:NROB scaling(2026-07-19 提交,job 53536–53541)
现在无法区分两种解释:
1. human 提供**跨具身运动知识**(我们要的结论)
2. human 只是**补了数据量**(robot-only 100 条 vs +human 额外 1800 条)—— 平凡解释

审稿人必问。跑 robot-only 在 NROB=100/300/900/all 的 scaling 曲线:
- 若曲线追平 +human → human-helps 是数据量效应,结论要大幅弱化
- 若曲线饱和在 +human 之下 → 跨具身知识站得住

`ACTION={skel,dummy5} MIX=r NROB={300,900,all} SPLIT=okfirst`,
输出 `outputs/cross_embodiment_wm/dualview_wm_skelact/{ACTION}_r_n{N}/`,日志 `logs/nrob_scaling/`。

### 待办
- [ ] NROB scaling 结果 + 曲线图,判决 human-helps 的机制
- [ ] can 抽 ≥68 帧评估序列,做合法的 44 步 rollout(对齐 v3 口径)
- [ ] 修 `rollout_dual` 的末帧重复填充(改因果窗口 或 断言 h+K+F ≤ L)
- [ ] 已发布的 can 44 步端到端数字带填充污染(第 25 步后),重跑前不要引用绝对值;
      四臂受同样污染,**相对比较仍有效**

### 追加(同日):一处推断错误的纠正 + 网格补齐

我一度写下"全量下加 human + 换 skel 是净负",依据是 `skel_rh_nall`(3.43)对比
`dualview_wm` 全量 dummy5 r-only(3.00)。**这两个格子同时差了两个变量**(action 表示 + 是否混 human),
不能这样比。**该推断作废。**

要判断全量下 skel 还赢不赢、human 还帮不帮,缺 `dummy5_rh_nall` 和 `skel_r_nall`。
已补提交 rh 半边(job 53543–53548),完整网格 = 2(action) × 2(mix) × 4(NROB):

| | n100 | n300 | n900 | nall |
|---|---|---|---|---|
| dummy5 r-only | 9.45 | 53539 | 53540 | 53541 |
| dummy5 +human | 6.36 | 53546 | 53547 | 53548 |
| skel r-only | 11.08 | 53536 | 53537 | 53538 |
| skel +human | 5.69 | 53543 | 53544 | 53545(对照已有 nall=3.43) |

这张表同时回答:
1. human-helps 是否只是数据量效应(r-only 曲线在哪个 N 追平 +human)
2. skel 同构在全量下是否仍赢 dummy5(22 号只在 n100 证过)
3. human 帮助随数据量的衰减速率

**22 号那批 gif 的定位要写清楚:它证明的是"在 robot 数据稀缺(100 条)时,
② 的 human-helps 能一路传到最终像素,且 skel+human 最好(obj 0.217)"。
这个结论在它自己的设定内成立,但适用区间未测。**

---

## 2026-07-19 NROB scaling 结果:★human-helps 只在 n100 成立,n≥300 反转(单 seed,复核中)

drift px cam_high(H=40,held-out n=150,SPLIT=okfirst,seed=0):

| | n100 | n300 | n900 | nall(2440) |
|---|---|---|---|---|
| dummy5 r-only | 9.45 | 4.72 | 4.92 | **3.00** ← 16 格最优 |
| dummy5 +human | 6.36 | 7.38 | 8.47 | 7.27 |
| skel r-only | 11.08 | 4.50 | 5.98 | 3.33 |
| skel +human | 5.69 | 5.37 | 5.97 | 3.43 |

### 三个发现

1. **human-helps 只在 n100 这一个点成立。** skel:n100 时 human 帮 11.08→5.69;
   **n300 时 r-only(4.50)已反超 +human(5.37)**;全量 r-only 3.33 vs +human 3.43,human 略有害。
2. **dummy5 混 human 随数据量单调变差:6.36 → 7.38 → 8.47(nall 7.27)。**
   robot 数据越多,加 human 越糟 —— 是**负迁移**,不只是"帮助减弱"。
   而 dummy5 r-only 在 300 条就到 4.72,优于任何 +human 格子。
3. **全量 robot-only dummy5 = 3.00 是 16 格全局最优**,比所有混 human 配置都好。

### ⚠️ 但曲线非单调 → 单 seed 噪声与效应同量级,暂不定论

`skel_r`:4.50(300) → 5.98(900) → 3.33(all);`dummy5_r`:4.72 → 4.92。
数据变多不该变差。用单 seed 的非单调曲线宣布"human-helps 不成立",
和之前用单点 n100 宣布"human-helps 成立"是**同一类错误**。

→ 已提交 16 个复核 job:seed∈{1,2} × 4 arm × N∈{300, all}(n300=翻转点,nall=最终结论)。

### 脚本改动(向后兼容)

`exp_scel_dualview_wm.py` 加 `SEED` env:
- `train_dual(..., seed=SEED)`;输出路径加 `_s{SEED}` 后缀(SEED=0 时路径不变)
- `subsample_robot_pool` 的 `rng(0)` → `rng(SEED)`:换 seed 才换"挑哪 N 条"
  (稀缺协议下子集选择本身是主要方差来源);**同一 seed 内所有 arm 仍共享同一子集**(控制变量)
- SEED=0 行为与改动前完全一致

### 对已发布结论的影响(重要)

- **22 号那批 gif 的结论**("② 的 human-helps 传到像素,skel+human 最好 obj 0.217")
  **在它自己的设定(NROB=100)内仍然成立**,但现在知道这个设定是 human 唯一有用的点。
  论文里必须写明适用区间,不能泛化成"human 数据帮助 robot world model"。
- 若复核确认反转,**整个 human-helps 主张要从"human 帮助 robot WM"降级为
  "robot 数据极度稀缺(≤100 条,约全量的 4%)时,human 数据可部分替代"**。
  这是个弱得多但仍然成立的主张,而且更诚实。

---

## 2026-07-19 同构骨架 skel-v2 的几何核验:名义同构,实际三项失配

用户眼检指出"拓扑不对齐,形状不对,human 两指不对称,最后那个点也没对齐"。量化后**全部成立**:

| 量 | robot | human | 判定 |
|---|---|---|---|
| 腕→指尖A 长度 | 25.63px | 35.92px | |
| 腕→指尖B 长度 | 24.53px | 28.16px | |
| **两指对称比(长/短)** | **1.04** [1.00,1.11] | **1.27** [1.18,1.40] | **分布几乎不重叠** |
| **腕→槽位3 长度** | 8.01px [6.07,10.59] | **19.20px [19.20,19.20]** | **human 是常数!** |
| 槽位3 夹角 | 38.1° [10.7,65.9] | 25.5° [19.1,33.3] | 差 12.6° |
| 指尖间距 中位 | 9.3px | 15.8px | ~1.7× |

### 三个确证的失配

1. **两指不对称**:robot 是机械爪,两指等长(1.04);human 拇指比食指短 27%(1.27)。
   两域分布几乎不重叠 → **模型光靠这一个数就能区分域**,与"两域落同一空间"的目标正好相反。
2. **槽位3 根本不是同一个东西**:`augment_clips_skeleton.py:stub_2d` 是
   `anchor2d + FOREARM_STUB_D * d2`(单位向量 × 写死的 0.15 ≈ 19.2px)。
   **human 的前臂根是固定长度的合成占位符,方向真实、长度是常数**;
   robot 的 link_5 是真实机械连杆(8px,随姿态 6–10.6px 变化)。长度维度无任何跨域信息。
3. **整体量纲差 ~1.7×**,且 robot 闭合时两指尖退化重合(0.0px),human 最小仍有 3.1px。

→ **四个槽位里只有腕点是真正对齐的。** 这很可能同时解释了
`skel` 的 robot-only 基线为何反比 dummy5 差(11.08 vs 9.45,引入噪声槽位),以及负迁移。

中间产物可视化 + README 已上传 Drive `23_★同构骨架skel-v2_中间产物核验/`
(脚本 `viz_skel_isomorphic.py`,输出 `outputs/cross_embodiment_wm/skel_isomorphic_qc/`)。
⚠️ 其中 `02_开合信号对照.png` 的 r=1.000 是**同义反复**(v2 指尖点就是从 grip 几何构造的),
只证明构造代码没写错,**不能当作"骨架捕捉真实抓握"的证据**。

## skel-v3(尺度不变结构表示):probe 判负,下游仍在跑

设计(token 数与维度不变 4×2,模型结构无需改):

| token | v2 | v3 | 针对 |
|---|---|---|---|
| t0 | 腕绝对位置 | 腕绝对位置(**保留**,定位精度必需) | — |
| t1 | 指尖A 绝对 | 手轴**单位向量**(腕→指尖中点) | 消量纲 |
| t2 | 指尖B 绝对 | 前臂**单位向量**(丢长度) | 消槽位3 常数长度 |
| t3 | 前臂根 绝对 | (归一化开合度, 归一化手尺度) | 消对称性差异 |

实现细节:`fwd_dual` 原本对所有 action token 减物体质心;v3 下**只有 t0 减质心**
(t1/t2 是单位向量、t3 是标量对,减质心会破坏语义)。归一化常数 = 各域各视角训练数据分位数。

### ❌ probe 判据:失败,而且更糟

收敛 LogReg + 留出集(遵循 [[feedback-probe-must-converge]]),每域 20000 帧:

| | 整体 probe | t1轴y | t2前臂x | t2前臂y |
|---|---|---|---|---|
| skel-v2 | 0.9998 | 0.708 | 0.494 | 0.499 |
| **skel-v3** | **0.9999** | 0.972 | 0.692 | **0.999** |

**原因很根本:前臂方向本身就是域身份的强信号** —— robot 是固定基座的机械臂,总从同一侧伸入;
human 手臂从另一角度进来。这是物理布置决定的,表示设计消不掉。
v2 里这个信息被长度/位置变化稀释(单维 0.499),**v3 归一化掉长度后,纯方向差异裸露成 0.999**。
我提纯错了东西。

### 但仍跑下游,因为 probe 不是判据

有明确前例:AMPLIFY 密集网格 probe 判 0.82 红灯,下游 human-helps 照样 +39~47%
([[project_amplify_grid_probe]])。教训是"可分性只是代理,不是判据"。
已提交 6 个 job:`ACTION=skelv3 × MIX{r,rh} × NROB{100,300,all}`。

### 这里浮现出一个根本张力(待验证)

真正同域的量是**相对物体的运动量**(接触点 velocity,probe≈0.50,见 [[project_action_dflow_separability]]),
但 velocity 表示之前试过,**丢掉绝对定位后精度输了**([[project_velocity_action_featurization]])。
形成 **同域 ⟷ 精度 的 Pareto**。v3 试图两者兼得(t0 保绝对 + t1~t3 保结构),probe 说没做到。
下游结果会告诉我们这个 Pareto 是否真的不可破。

---

## 2026-07-19 转向:action representation 探索(用户拉回主线)

用户明确:scaling 那条是防守性工作,不是要证的东西。主线 = **(1) 做一个好的 action representation
(2) 让 human 在 ② 之外帮更多**。

### 框架性判断:human 帮助的上限 = ② 承担的职责比例

human 帮 ② 而帮不了 ③ 不是偶然,是结构性的:**flow 域不变,外观域相关**。
③ 干的活(机械爪长什么样、金属反光)human 数据里没有对应物。
→ 让 human 帮更多只有两条路:把域不变的能力从 ③ 移到 ②(② 现在只吐 48 个 2D 点,太薄);
或新增域不变组件。用户选了 **(1) 光栅化 action** 和 **(3) action-free 物体动力学先验**。

### 澄清用户的质疑:flowskel 的大胜是真的,和 NROB=100 无关

用户问"③ 之前的大胜是不是也只在 N_rob=100 才对"。核查:③ 的实验脚本
`exp_scel_dualview_dit_formal.py` **根本没有 NROB 参数**,用全量 robot(2440):

| | seed0 | seed1 | seed2 |
|---|---|---|---|
| flow(3 通道) | 0.263 | 0.252 | 0.252 |
| flowskel(4 通道) | 0.194 | 0.193 | 0.191 |

三 seed 完全不重叠,差距约 2σ 的 8 倍,且是 replay 口径(GT flow,不经 ②)。
会混淆是因为 **22 号 gif 把全量训练的 ③ 和 n100 削减的 ② 拼在了一起**。

### OSCAR 精读判决(2606.04463v2,全文 26 页)

| 问题 | 答案 |
|---|---|
| 有没有做同域对齐? | **完全没有**。无 alignment / canonicalization / OT / domain adaptation |
| 怎么处理形态不匹配? | 只有一句叙事性论述,**零实证**(无 probe、无分布分析、无 gap 量化) |
| 绝对还是相对坐标? | **绝对像素**;相对(frame-to-frame delta)是他们**跑输**的 latent-action baseline(PSNR 19.22 vs 23.48) |
| action 表示形式 | URDF 全部 link origin + MANO 全部关节,**光栅化成黑底线画图**,过 VAE 与视频 latent 相加 |
| human 帮多少? | PSNR 23.48→24.24,LPIPS 0.106→0.094(warm-start 最好);**无 scaling 实验、无负迁移检查** |

**核心 takeaway:OSCAR 把"同域"做在渲染/格式层,不是特征/分布层。点数不必相同,因为光栅化。**
这直接解除了我们 skel-v2 被迫做点配对的约束。

### 实现:agent-centric 局部光栅化(不能照搬全图版)

先做全图版,眼检+量化发现会翻车:robot 链包围盒 **1.12×0.46 幅**(link_1/2/3 画内率 **0%**,基座在框外),
human 手只有 **0.23×0.13 幅** —— 差 5×,两域光栅图统计完全不同,**换个形式重蹈 skel-v2 覆辙**。
根因:OSCAR 相机看得到整个机器人,我们的 crop 只框工作区。

→ 改 **以末端为中心、半径 0.30 的局部窗口**(依据:robot link_1/2/3 距末端 0.80–0.94 自动出窗,
link_4/5/6+两指尖 ≤0.23 在窗内;human 全部 21 关节 p95=0.27 在窗内)。
点数仍不同(**5 vs 21**)—— 这正是光栅化允许的。白像素占比 0.048 vs 0.068(1.4×,原 5×)。

**这也正是用户问的"相对+绝对结合"**:相对部分用光栅图(免配对),绝对部分用末端位置向量(保精度)。

新文件:`augment_clips_fullchain.py`(chain sidecar + `rasterize_local`)、
`gen_raster_sidecar.py`(预计算 uint8)、`viz_fullchain_raster.py`(QC 图)。
`exp_scel_dualview_wm.py` 新增 `ACTION=raster`:打包 `(N,L,513,2)`=[末端位置]+[32×32 光栅图],
复用现有窗口索引/填充逻辑,**主训练循环零改动**;`RasterAct` CNN 0.34M 参数。
形状+梯度验证通过(光栅 CNN 首层梯度范数 4064,条件确被使用)。

### ⚠️ probe 三连否定 → 结论:别再用 probe 筛设计

| 表示 | probe(两域可分性) |
|---|---|
| skel-v2 4 点绝对坐标 | 0.9994 |
| skel-v3 尺度不变结构量 | 0.9999 |
| 局部光栅图 32×32 | 0.9996 |

**human 手和 robot 爪形状本来就不同,任何保留形状的表示必然可分 —— 这是物理事实,不是设计缺陷。**
而 OSCAR 从没同域也成功了。加上我们自己的前例(AMPLIFY 网格 probe 红灯但下游 +39~47%),
**"同域"可能根本不是正确的目标**。今后不用 probe 做设计筛选,只做诊断记录。

### 排队中

`ACTION=raster × MIX{r,rh} × NROB{100,300,all}` 6 job;
另有 16 个 seed 复核 + 6 个 skelv3,共 28 个在队列。

### 待做

- [ ] 方向 3:action-free 物体动力学先验(human 只教"物体受力后怎么动",完全不用 human 的 agent 表示)
- [ ] 把 ② 做厚(预测物体 6D 位姿/接触状态),抬高 human 帮助的天花板

---

## 2026-07-20 光栅图质量修复 + grip 诊断("是不是 2D 的锅")

### 用户指出光栅图三个质量问题, 全部修复
1. "每个点是十字" = `cv2.circle(半径1,实心)` 在整数坐标退化成加号 -> 改抗锯齿圆点(LINE_AA+SHIFT=4 亚像素)
2. "密集处挤成一坨" = 32px 太低 -> 升 64px
3. "反应不出 grip" -> 见下方诊断

64px 后两域白像素占比 0.0996 vs 0.0957(32px 是 0.048/0.068, 全图版差 5×), 格式统计更同域。
质量对比图 `05_★质量对比_grip.png`。

### ★grip 诊断: 不是 2D 的锅, 是 robot 夹爪 3D 开合本就小

用户直觉: 抓同一个 can 时人手和 robot 开合应相近, 2D 看不出是不是投影丢信息?
实测(eef3d 3D + eef 2D + cam 外参算深度):

| | robot | human |
|---|---|---|
| 到 cam_high 深度 | 0.685m | 0.662m |
| 投影比 | 0.276 px/mm | 0.262 px/mm |
| 3D 指尖开合 | **28.4mm** | **60.0mm(≈罐径66)** |
| 2D 投影开合 | 8.0px | 15.7px |

**结论: 深度几乎相同, 投影比相同 -> 不是 2D 投影的锅。** 真相是 robot 的两指尖(URDF fingertip
link 原点)3D 开合本就只有 human(MANO 指尖)的一半 —— 取点几何 + 夹爪机械尺度决定。
robot grip 弱 = 它 3D 上就小(28mm→8px), 光栅化再当成末端一小段。
诊断图 `06_★grip诊断_2d3d深度.png`(已发用户 + Drive)。

### 解法: 显式归一化抓握程度标量(ACTION=rasterg)

两域"抓握程度"(各自从张到合抓同一 can)是相近动作, 差的是绝对尺度。
-> 补一个归一化标量: 2D 指尖间距按各域各视角 p05-p95 映射到 [0,1](复用 SKELV3_GAP),
抓住罐子时两域 ≈ 同一值。**不动位置**(skelv3 归一化位置丢精度已判死, 这是关键区别)。
raster = 光栅图(构型,免配对) + 末端位置(定位) + 归一化 grip(开合), 三者互补。

`ACTION=rasterg`: 打包 (N,L,2050,2)=[位置]+[(grip,0)]+[64² 光栅图]; act_pos 输入加 grip 维;
不影响正在跑的 raster(纯光栅) job -> 干净消融 raster vs rasterg = grip 标量的净贡献。
形状+梯度验证通过(grip∈[0,1] 中位 0.617, 三组件梯度均非零)。

### 消融链(全部 H=20 合法口径, drift px)

| action | r-all(全量) | 说明 |
|---|---|---|
| dummy5 | 2.32 | 基线 |
| skel-v2 | 2.56 | 同构失配 |
| skelv3 | 3.73 | **判死**(归一化位置毁精度) |
| raster-32px | 2.09 | 全量最好, 但混 human 不稳(rh-300=7.11) |
| raster-64px | 跑中(53667-72) | 隔离分辨率变量 |
| rasterg-64px | 跑中(53673+) | +归一化 grip 标量 |

待 raster-64px / rasterg-64px 出结果: 看 (a) 64px 是否修好 rh 不稳 (b) grip 标量净贡献。

---

## 2026-07-20 (关键修正) human-helps 混训 bug + 最终 scaling 曲线

### 我来回翻转的根因: exp_scel_dualview_wm 混训有 bug
原实现 `train_dual` 的 MIX=rh: 先训完**所有** robot batch, 再单独训**所有** human batch。
稀缺时 robot 300 条(~5 batch) vs human 1795 条(~28 batch), human batch 全堆在每个 epoch 结尾
-> 梯度被 human 主导 -> robot heldout drift 变差 -> **假的"human 有害"**。
我的 nrob_scaling 全部基于此 bug, 得出"human helps 站不住"的错误结论并来回翻转。

**正确答案一直在另一个 session 的 curves.json**(exp_scel_dualview_comb, 统一批, 多 seed)。

### ★human-helps 最终结论(can_dual, drift px@128, H=20, 多 seed, 统一批)

| robot N | 占全量 | ro | rh | Δ=ro−rh |
|---|---|---|---|---|
| 50 | 2% | 9.86 | 4.95 | **+4.91** |
| 100 | 4% | 8.68 | 3.98 | **+4.70** |
| 400 | 16% | 4.78 | 3.44 | **+1.34** |
| 800 | 33% | 3.15 | 2.57 | +0.58 |
| 1600 | 65% | 2.43 | 2.26 | +0.17 |
| 2460 | 100% | 1.98 | 2.10 | **−0.12(耗尽)** |

**human-helps 真实, 但"稀缺专属": 随 robot 数据量单调衰减, 全量耗尽(≈0)。**
用户 N=400 不够的直觉正确(才 16%, 还在稀缺区)。图 `humanhelps_scaling_FINAL.png`(已发用户)。
命题定位: 不是"human 让 robot WM 更好"(全量不成立), 而是"robot 稀缺时 human 高效替代"(数据效率)。

### 修复(2026-07-20)
`_ss_batch_step` 拆出 `_ss_loss`(不 step); MIX=rh 改成**每个 optimizer step 同时含
1 robot batch + 1 human batch 的梯度**(robot batch 少则 cycle 复用, 稀缺 robot 不被淹没)。
robot-only 路径保持 byte-identical。取消所有基于旧 bug 的 raster/rasterg job。
验证 job: FIX_dummy5_{r,rh}_n300 -> 预期 rh-n300 从(bug)5.4 恢复到 ~3.4(对齐 curves.json)。

### 表示分辨率澄清(用户问)
- dummy5/skel/skelv3 = **坐标点向量(float, 无分辨率概念)**, 精确无量化。
- raster/rasterg = 光栅**图**, 有分辨率(32/64/128)。上 128 本质是减少量化损失去**追赶**
  dummy5 本就有的精度 -> raster 的价值在"格式统一免配对"帮 human, 不在精度碾压。
- 128 内存: 按现打包法(塞进 action token (...,2)) robot 单视角 34GB 会爆; 须改单独 uint8 存+训练切片。
  建议先验证 64 修复后能否帮 human, 再决定是否付 128 工程成本。

---

## 2026-07-20 action 表示 pilot 定论 + 综合可视化对比

### ★最终判决: dummy5 完胜, 4 种 agent 表示全部不赢
pilot (修好混训, H=20, N=100, drift px cam_high):

| 表示 | robot-only | +human(判据) | Δ |
|---|---|---|---|
| **dummy5** | 7.78 | **3.03** ← 最好 | +4.74 |
| raster | 9.01 | 3.54 | +5.47 |
| rasterg | 10.00 | 3.72 | +6.28 |

skel-v2/skelv3 之前已判负。**rh 绝对精度 dummy5(3.03) < raster(3.54) < rasterg(3.72)。**
raster/rasterg 的 Δ 更大是"基线烂"假象(同 skel 陷阱), rh 绝对精度全输。

### 用户洞察被量化坐实: 光栅化的"噪声"是根因
- raster 局部光栅图(64px, 窗口半径 0.30)的构型量化噪声 = **±0.60px@128**(一个像素跨 1.2px)
- dummy5 是精确连续坐标, **零量化噪声**
- robot-only 代价正好对上: raster 9.01 vs dummy5 7.78(差 1.23px = 噪声的账)
- rasterg(加 grip 标量)比 raster 还差 -> grip 诊断对但补标量没帮上("修 artifact ≠ 帮下游"第三次: canon5/skelv3/rasterg)

### 干净结论(4 种表示全试完)
| 表示 | 输的原因 |
|---|---|
| skel-v2(同构) | 几何失配(两指不对称/槽位3常数) |
| skelv3(尺度不变) | 归一化位置毁精度 |
| raster(光栅) | 量化噪声 |
| rasterg(光栅+grip) | 噪声 + grip 标量无用 |

**human-helps 不需要 agent 表示同构/对齐**(dummy5 本身两域不对齐也赢)。
表示越精确越好; 任何为"对齐/同构"牺牲精度或引入噪声的改造都是负的。
与既有发现自洽: probe 说同域非必要 / AMPLIFY 网格 probe 红灯但下游帮 / human-helps 走 object-flow。
**action 表示这条线到头 -> dummy5 定为最终 action 表示。**

### 综合可视化: viz_all_action_reps.py
5 列(dummy5/skel-v2/skelv3/raster/rasterg)× cam_high 帧 + action-rep overlay(青)+ ②rollout
预测(红) vs GT(绿), r-only n100, H=40, 6 个 held-out seq。
输出 outputs/cross_embodiment_wm/action_reps_compare/。
