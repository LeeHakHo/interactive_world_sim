# 跨具身世界模型 — human 数据帮 robot:完整回顾(2026-07-20)

> 本文档自包含,写给汇报/做 slides 用。核心问题:**human 演示数据能不能帮训练 robot 的世界模型?怎么帮、帮多少、用什么表示。**
> 数据集:can_dual(双视角机器人抓罐子 + 人手抓罐子);② = 物体运动预测(flow);③ = 渲染。
> 所有相关 gif/图见文末"交付清单"及 Google Drive。

---

## 0. 一句话结论(先看这个)

**human 数据的价值真实存在,但有明确条件:**
1. **只帮 ②(运动预测),不帮 ③(渲染)** —— 渲染是外观、域相关,human 帮不上(原理性)。
2. **只在 robot 数据稀缺时有用**,随 robot 数据量增加单调衰减,数据充足后**耗尽**(≈0)。
3. **机制是共享的物体动力学**("物体被推怎么动"),与 agent 表示形式无关 —— human 把误差砍掉固定比例(~61%),不管用什么 action 表示。
4. **价值定位 = 数据效率**:robot 稀缺时,便宜的 human 视频能高效替代一部分昂贵的 robot 遥操作数据(~3–5 条 human ≈ 1 条 robot,human 采集快 4–5×)。

**不成立的强命题**:"human 数据让 robot 世界模型更好"(数据充足时耗尽)。
**站得住的命题**:"robot 数据稀缺时(新任务/新机器人只有少量 demo),用 human 视频补比硬采 robot 划算"。这与 OSCAR(2606.04463)用 human 数据的动机一致。

---

## 1. human-helps 的完整曲线(核心结果)

**实验**:robot-only ② vs robot+human ② 混训,在不同 robot 数据量 N 下比 held-out drift(预测物体点 vs 真值,像素,越低越好)。can_dual,H=20,多 seed。

| robot 数据量 N | 占全量 | ro(纯robot) | rh(+human) | **Δ = ro−rh** |
|---|---|---|---|---|
| 50 | 2% | 9.86 | 4.95 | **+4.91** 大帮 |
| 100 | 4% | 8.68 | 3.98 | **+4.70** 大帮 |
| 400 | 16% | 4.78 | 3.44 | **+1.34** 帮 |
| 800 | 33% | 3.15 | 2.57 | +0.58 小帮 |
| 1600 | 65% | 2.43 | 2.26 | +0.17 微帮 |
| 2460 | 100% | 1.98 | 2.10 | **−0.12 耗尽** |

**结论:human-helps 是"稀缺专属",随数据量单调衰减,全量耗尽。** 图:`humanhelps_scaling_FINAL.png`。

### 1.1 一个关键的 bug 修复(否则结论会反)

最初我的混训实现有 bug:**先训完所有 robot batch,再单独训所有 human batch**。稀缺时 human batch 数是 robot 的 ~6 倍,全堆在 epoch 结尾 → 梯度被 human 主导 → robot 精度反而变差 → **假的"human 有害"**。

**修复**:每个 optimizer step 同时含 robot + human 梯度(一次 backward)。
**验证**:dummy5 rh-n300 从 5.43(bug,有害)→ 2.71(修复,human 帮),Δ 从 −1.46 翻正到 +0.93。
教训:混训方式对 human-helps 结论是决定性的。

---

## 2. human 帮 ②(运动)不帮 ③(渲染)

| 层 | human 帮吗 | 为什么 |
|---|---|---|
| ② 物体运动预测 | ✅ 帮(见 §1) | 物体怎么被推动 = **域不变**的物理,human/robot 共享 |
| ③ 渲染外观 | ❌ 不帮 | 爪子/手长什么样 = **域相关**,human 数据里没有 robot 外观 |

③ 不帮是**原理性**的(不是没做好),keyboard demo 和 dual-view DiT 渲染器都独立确认(rh ≈ ro)。
**所以 human-helps 的唯一战场是 ②(object-flow)。**

### 机制:human 教"物体会动"

稀缺 robot-only 模型只预测出真实速度的 **26%**(罐子该动它不敢动);混 human 后拉到 **74%** —— human 数据教会模型"物体真的会被推走"。这是比抽象 drift 更直观的机制证据。

---

## 3. action 表示探索:4 种改造全部试完

**动机**:能不能设计更好的 agent 动作表示,放大 human 的帮助?

| 表示 | 设计 | 结果 |
|---|---|---|
| **dummy5**(基线) | eef 3 点 → 5 点虚拟星座(精确坐标) | 稀缺最好 |
| skel-v2(同构) | robot 4 关节配对 human 4 关节 | ❌ 几何失配:两指对称比 robot 1.04 vs human 1.27(分布不重叠)、前臂根是写死 19.2px 常数占位符 |
| skelv3(尺度不变) | 单位向量 + 归一化标量 | ❌ 归一化位置毁精度 |
| raster(光栅) | 完整运动链光栅化成 2D 线画图(OSCAR 式,免点配对) | ❌ 量化噪声 ±0.6px |
| rasterg(光栅+grip) | 光栅 + 归一化抓握标量 | ❌ 噪声 + grip 标量无用 |

### 3.1 ★重大发现:表示优劣依赖数据量(反转)

| 表示 | **稀缺 n100** | **全量 nall** |
|---|---|---|
| dummy5 | **7.78** 最好 | 2.32(第三) |
| skel-v2 | 8.66 | 2.56 |
| skelv3 | 10.50 | 3.73 |
| raster | 9.01 | 2.13 |
| rasterg | 10.00 最差 | **1.96** 最好 |

**稀缺时精确坐标(dummy5)赢;充足时光栅化(rasterg)反超。**

机制:光栅化的量化噪声在稀缺时主导 → dummy5 精确坐标数据高效;充足时大数据把噪声压下去,光栅化更丰富的构型信息发挥作用。**印证 OSCAR:他们用光栅化正是因为有大规模数据(85k+95k episodes)。光栅化不是坏表示,是"数据饥渴"。**

### 3.2 ★human 的相对作用与表示无关(~61%)

| 表示 | robot-only | +human | 绝对 Δ | **相对改善%** |
|---|---|---|---|---|
| dummy5 | 7.78 | 3.03 | +4.74 | **61%** |
| raster | 9.01 | 3.54 | +5.47 | **61%** |
| rasterg | 10.00 | 3.72 | +6.28 | **63%** |

**human 把误差砍掉的比例几乎恒定(~61%),不管用什么 action 表示。** 绝对 Δ 的差异只是各表示 robot-only 基线不同造成的假象。
**原因:human 提供的是物体动力学知识,与 agent 表示形式无关。** → human-helps 走物体动力学,不走 agent 表示对齐。

### 3.3 结论:human-helps 场景用 dummy5

human-helps 只在 robot 稀缺时有用,而稀缺时 dummy5 最优 → **用 dummy5 + human 混训**。
更有价值的 paper 论点:**表示选择依赖数据规模(稀缺用精确坐标 / 充足用光栅化),与 OSCAR 互为印证;而 human 的价值来自共享物体物理,不来自 agent 表示设计。**

---

## 4. grip 诊断:为什么光栅图反映不出抓握

**问题**:光栅图里 robot 的夹爪开合(grip)很弱。是 2D 观测丢信息吗?

| | robot | human |
|---|---|---|
| 到相机深度 | 0.685m | 0.662m(几乎相同)|
| 投影比 | 0.276 px/mm | 0.262 px/mm(相同)|
| 3D 指尖开合 | **28.4mm** | **60.0mm(≈罐径66)** |

**不是 2D 的锅**(两域深度、投影比都相同)。真相:**robot 的两指尖(URDF fingertip link)3D 开合本就只有 human(MANO 指尖)的一半** —— 取点几何 + 夹爪机械尺度决定。robot 的 grip 弱是因为它 3D 上就小,不是投影问题。图:`fullchain_raster_qc/06_★grip诊断_2d3d深度.png`。

---

## 5. horizon:rollout 想滚多久滚多久

**关键认识(修正一个误解)**:`rollout_dual`(②)是**自回归**的,动作序列到头后默认停住(重复末帧),模型照样稳定往下滚,**不漂移**。
- **单 clip 上限 = 44 步**(GT track 长度限制)
- **keyboard 合成动作 = 理论无限**(每步现合成,只受累积漂移)

铁证:e2e 的 ② 纯自回归预测,**obj-LPIPS 0.187@H40 ≈ 0.191@H20**,自预测 40 步不漂移。
之前误以为"动作窗口填充"是硬限制(H≤24),其实填充只是"物体默认停",自回归下完全稳定。

---

## 6. 参考的论文

- **OSCAR(2606.04463v2)** — 跨具身机器人视频世界模型。用 **2D 骨架光栅化**作统一条件(URDF/MANO 全部关节 → 线画图),**无任何显式对齐/归一化**,靠格式统一 + 混训 + warm-start。绝对像素坐标(相对坐标是他们跑输的 baseline)。+human 帮 PSNR 23.48→24.24,但**无 data-scaling 实验、无负迁移检查**。→ 我们的 scaling 曲线 + 表示×数据量反转是对 OSCAR 的补充。
- **LaST-HD / EgoWAM** — combine-action 线的对齐机制来源(另一 session)。

---

## 7. 交付清单(gif + 图 + Drive 位置)

Drive 根:`iws_evals/2026-07-19_flow表示升级_骨架与warp/`

| 内容 | 本地路径 | Drive |
|---|---|---|
| ★human-helps 完整曲线 | `outputs/cross_embodiment_wm/humanhelps_scaling_FINAL.png` | 待传 |
| ★5 种 action 表示 rollout 对比(H=44) | `outputs/cross_embodiment_wm/action_reps_compare/*.gif` | `25_★所有action表示rollout对比/` |
| ★human-helps 表示×数据量二维图 | (sweep 完成后生成) | 待传 |
| grip 2D/3D 诊断 | `fullchain_raster_qc/06_★grip诊断_2d3d深度.png` | `24_★完整运动链光栅化_OSCAR式/` |
| 同构骨架失配核验 | `skel_isomorphic_qc/` | `23_★同构骨架skel-v2_中间产物核验/` |
| 完整运动链光栅化 | `fullchain_raster_qc/` | `24_★完整运动链光栅化_OSCAR式/` |

---

## 8. 一页 slides 的骨架建议

1. **问题**:human 视频能帮训练 robot 世界模型吗?
2. **答案**:能,但只在 robot 稀缺时,只帮运动(②)不帮渲染(③) → 曲线图
3. **机制**:共享物体动力学,与表示无关(~61%恒定) → 相对改善表
4. **表示**:稀缺用精确坐标(dummy5),充足用光栅化(OSCAR式) → 反转表
5. **定位**:数据效率(稀缺时 human 替代 robot),不是"让 WM 更强"
6. **对标**:补了 OSCAR 没做的 scaling + 负迁移分析
