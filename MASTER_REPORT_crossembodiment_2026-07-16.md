# 跨具身世界模型 — 完整分析报告（2026-07-12 至 07-16）

> 本文档自包含、可下载分析。写给用户做详细分析，也给下个 session 无缝接手。
> 涵盖：双机位渲染器正式化 → 数据体检与修复 → flow 表示升级（骨架条件） → human-helps 实验（②骨架动作 / latent 残差） → 端到端渲染验证。
> 配套活文档：`FLOW_WARP_REPR_LOG.md`（讨论逐条+候选表）、`DUALVIEW_DIT_REPORT.md`（正式化）、`CAN_DATA_AUDIT_2026-07-13.md`（体检）。
> 所有可视化在 Google Drive：`iws_evals/2026-07-1{3,4,5,6}_*` 与 `iws_evals/2026-07-16_flow表示升级_骨架与warp/`。
> 术语约定（全文不用内部黑话）：**②=运动预测模型**（给动作，预测物体上跟踪点的未来位置，代号 flow world model）；
> **③=渲染器**（给第0帧图像+条件，预测未来每帧的图像；先预测冻结VAE的隐变量latent，再由VAE解码器还原像素）。
> **两个机位**：v0=cam_high（俯视，视野干净）、v1=cam_low（低角度，机械臂常挡罐子）。
> **obj-LPIPS**：物体周围64×64小块的感知相似度，越小越像真的（下称"物体误差"）。**agent-LPIPS**：以机械臂/手骨架为中心的同款指标（下称"agent误差"）。**drift px**：② 预测点偏离真值的像素数。

---

## 0. 全局结论（先看这个）

1. **给渲染器"物体运动"作条件，显著优于给"机械臂动作数字"**（正式化主判据，3种子统计显著）。真 IWS 原版模型垫底。
2. **意外发现**：只画"手/骨架"的空间条件（不给物体运动）在俯视机位甚至反超——赢的一大块是"把条件画成图 vs 压成数字向量"这个机制差，物体运动内容的独有价值体现在端到端可预测性。
3. **★最强结果：给渲染器加一个"骨架线画"agent 条件通道，全面刷新纪录**（物体误差 0.256→0.193，agent 误差 0.296→0.214，3种子统计显著6-7倍），并让长时长渲染零退化。已采纳为默认。
4. **★human-helps 是真的，且穿过好渲染器成立**：② 用同构骨架动作 + 人手混训，最终渲染的物体误差从只机器人的 0.308 降到 0.217（−30%），骨架比旧动作表示（dummy5，−20%）更能榨取人手数据。latent 残差混训是第二条独立证据（−33%）。
5. **治糊的便宜赢家 = 延长训练**（60→120轮，0.194→0.157）；加大感知损失权重反而更糊。
6. **数据侧修了两个坑**：端到端评估的数据泄漏（②训练集混进22/24条测试）、人手真手腕缺失（改成 side-car 补充，74%有效）。
7. **未决 TODO**：物体跟踪点重撒（机器人成功 0.33→0.80，人手溢到托盘/蓝条待修，目前无实验在用）。

---

## 1. 数据与设置

- **数据集** `outputs/flow_render_dataset_can_dual/`：机器人 2700 段（每段48帧）+ 人手 1800 段（每段24帧），每段双机位（cam_high 俯视 + cam_low 低角度），48 个物体跟踪点 + 3 个末端执行器（eef）点，`low_valid` 掩码过滤低角度无效帧。任务=抓取推动一个可乐罐（红身银顶）。
- **③ 渲染骨干**：add 式注入的 DiT，维度384×8层×6头（此前 detmem-DiT 判决的赢家）；**冻结的16通道预训练 VAE**（ostris/vae-kl-f8-d16，全程不训）。双机位 token 联合注意力。
- **训练配方（所有对比臂一致）**：批量8，学习率2e-4，60轮，机器人用 latent-MSE + 解码后 LPIPS（权重1），人手用物体区像素 MSE；机器人+人手混训（rh）或只机器人（r）。每设置3个随机种子，差距超2倍合并标准差才称"显著"。
- **评估**：`obj_lpips_audit`（物体区64×64感知相似度+检测率防"物体消失"）+ PSNR，固定24段留出视频（`eval_seqs_n24.json`），所有臂共享。20帧主表 + 44帧压力测试。
- **② 运动预测模型**：`exp_scel_dualview_wm.py` 的 DualLWC（双机位联合48点flow预测，dummy5或骨架动作），干净split版 `dualview_wm_cleansplit/wm_dual.pt`。
- **关键环境坑**：conda `iws`（base 的 numpy2 崩 cv2）；GPU 任务走 SLURM sbatch；集群 bare `env` 是坏 shim 要用 `/usr/bin/env`；磁盘写 `/scr`（经 symlink），别写满 `/scr2`。

---

## 2. 参考论文与内部先例（每条注明对本工作的作用，来源已核实/继承标注）

| 来源 | 要点 | 对本工作 |
|---|---|---|
| **OSCAR**（arXiv 2606.04463，北大/密歇根/NVIDIA，一手核实） | 跨具身动作条件视频世界模型；机器人臂+人手都渲染成同一种2D骨架线画作空间条件；消融里"骨架空间条件最强"、"动作压成向量最差" | 骨架条件通道（flowskel）的直接设计来源；解释了 eefsp 反超；naive 基线设计对照 |
| **IWS stage2**（内部系统） | 动力学 CMLatentDynamics：三维卷积时空骨干 + 动作向量经MLP做每层FiLM特征缩放 | eeffilm 臂复刻其注入方式；外部基线整模块拿来独立训 |
| **EgoWAM**（Georgia Tech，一手核实网页+PDF） | 不用骨架条件；在**监督目标层**统一人机（共享trunk+辅助世界模型损失，推理时丢弃），非输入表示层；最佳目标=去相机自运动的3D flow，胜过像素VAE | 与我们骨架条件是**互补的另一根轴**（别当骨架的佐证）；其"重建目标把外观逼进trunk"独立解释了我们 flowwarp 为何没赢；提出新候选：flow目标做相机自运动稳定化（对静态can机位无用，对头戴人手数据有用） |
| **LaST-HD**（2606.23685，经另一 session 笔记继承） | 输入端同质化（骨架/a1）以牺牲单域精度换 human-helps Δ；只有目标端对齐（align_wm）是"免费"的 | 预设了 ②骨架动作实验的 Pareto 权衡：期望"骨架robot-only更差、但有人手后反超"——今晚正是这个模式 |
| **HaMeR / MANO**（手部重建） | 人手腕+拇指尖+食指尖三点；21点手模 | 人手骨架/真腕来源；wrist side-car 修复 |
| **AMPLIFY**（2502.20391，核实） | 密集网格flow（20×20全画面跟踪）+离散token；每帧重铺跟踪点防漂移 | 候选：密集网格flow（可分性检测未过但下游仍+39-47%，未判死）；稀疏→密集解码器思路 |
| **DexWM / MaskWAM**（调研文档） | DexWM：agent运动与物体运动分开通道。MaskWAM：运动只当输入条件不要预测输出→性能84.9%→21.6% | 候选：agent/物体分通道；渲染器加辅助预测头 |
| 内部 detmem-DiT 判决 | add式注入的确定性latent回归赢过flow-matching | ③ 骨干选型 |
| 内部 Task#21（latent残差） | 冻结VAE + 预测帧间Δz + 混训：残差>>直接，human帮robot（旧数据+35.8% vs +5.6%） | latent残差实验的移植原型 |

---

## 3. 正式化：flow 接口 vs naive action（3种子，20帧主表，obj-LPIPS 越小越好）

| 条件方式 | 俯视 v0 | 低角度 v1 | 说明 |
|---|---|---|---|
| **flow（物体运动画成图，ours）** | 0.256±0.007 | 0.279±0.003 | 物体48点位移splat |
| eefsp（只画手3点，画成图） | **0.228±0.012** | 0.291±0.014 | ≈OSCAR骨架空间条件；俯视反超 |
| eeffilm（手3点压成24维向量，IWS式FiLM注入） | 0.278±0.011 | 0.322±0.021 | =IWS注入方式复刻 |
| 真 IWS stage2（三维卷积+扩散，外部基线） | 0.324 | 0.371 | n=1，非controlled |

- **主判据PASS**：flow vs eeffilm 双机位差距均 >2倍标准差（v0 d=0.023>0.019，v1 d=0.043>0.030）——统计显著。
- **诚实nuance**：eefsp 俯视反超 flow（0.228 vs 0.256），说明优势一大块来自"空间图注入 vs 向量FiLM"机制；物体运动内容的独有价值靠端到端（下§4）撑。
- **cross-view 联合注意力无增益**（屏蔽机位间注意力后不变，负结果）。
- **human-helps 在③渲染层不帮**（flow/eefsp/eeffilm 三臂的 r vs rh 全部<2σ）——渲染外观是embodiment-specific，human帮在②。
- 完整表见 `outputs/cross_embodiment_wm/dualview_dit_formal/ablation_table.md`。Drive：01（三臂+IWS对比gif）、03/04（报告）。

---

## 4. 端到端 ②→③（去数据泄漏后，obj/agent-LPIPS）

| 列（20帧 / 44帧） | 物体 v0/v1 | agent v0/v1 |
|---|---|---|
| flow基线：真实运动→③（天花板） | 0.263/0.282 · 0.265/0.284 | 0.296/0.311 · 0.305/0.323 |
| flow基线：②预测运动→③（端到端） | 0.270/0.298 · 0.277/0.305 | 0.306/0.323 · 0.320/0.340 |
| naive（eef-FiLM）→③ | 0.285/0.345 · 0.282/0.333 | 0.301/0.356 · 0.304/0.353 |
| **骨架：真实运动→③（天花板）** | 0.194/0.229 · 0.193/0.227 | 0.214/0.251 · 0.214/0.252 |
| **骨架：②预测运动→③（部署配置）** | **0.202/0.246 · 0.209/0.253** | 0.223/0.267 · 0.231/0.275 |

- ② 预测误差（drift）：俯视2.71px/低角度3.67px（20帧）、3.85/4.93px（44帧），均过4px验收线。
- **骨架臂部署配置（②预测+动作画骨架，零预测零泄漏）好于所有基线的作弊天花板**；距自身天花板仅0.008-0.026。骨架通道不需要预测，从replay到部署几乎无损，这是flow通道给不了的鲁棒性。
- Drive：11/12（端到端6列gif 20/44帧）、13（指标表）。

---

## 5. ★核心：flow 表示升级 = 骨架 agent 条件通道

### 5.1 动机与设计
用户观察：现有渲染都有点糊；agent（机械臂/手）画得像团黑影。诊断：双机位正式版渲染器**没有 agent 条件通道**（旧单机位线有，移植时丢了），条件只有3通道（物体运动dx/dy+足迹）。
设计：加**第4通道=OSCAR式骨架线画**（纯几何画线，零学习零监督，误差只剩动作本身；从关节/关键点轨迹生成=可部署、零泄漏）。机器人用整臂正运动学9连杆链投影连线（线宽随夹爪开度），人手用真腕+两指尖+前臂短线。

### 5.2 结果（3种子，20帧，obj-LPIPS）
| 臂 | 俯视 v0 | 低角度 v1 | agent v0/v1 | PSNR |
|---|---|---|---|---|
| flow基线（无agent通道） | 0.256 | 0.279 | 0.296/0.311 | 22.7/22.1 |
| **flowskel（+整臂骨架）** | **0.1925±0.0014** | **0.2318±0.0039** | 0.214/0.251 | **24.95/23.75** |
| flowskel3（+只手骨架，消融） | 0.199 | 0.240 | 0.219/0.261 | 24.6/23.3 |
| flowwarp（像素搬运，对照） | 0.271 | 0.282 | — | 22.5/22.1 |

- **骨架通道全面刷新纪录**：对基线差距0.063/0.047 = 显著性门槛的6-7倍；种子方差全场最小（±0.0014，训练最稳）。
- **只手 vs 整臂近平**（0.199 vs 0.194）→ 主效应是"画成线"这个形式，整臂链边际贡献小（数据里臂基座常在裁剪框外）。
- **长时长零退化**：44帧下 flowskel 0.193/0.227（与20帧持平），基线在退化——agent 被确定性骨架锚定，自回归误差不再通过agent区累积。这是长时程稳定性论据。
- Drive：05/06（长版三臂对比gif）、07/08（新臂对比20/44帧）、09（骨架通道三联图）。

### 5.3 舍弃项：flowwarp（像素搬运）
把物体运动当"搬运算子"直接搬第0帧像素（而非条件），网络补洞。结果0.271平局偏负+ghost伪影+眼检不优。与v3方块判决一致（"纹理战场"假说未兑现），骨架通道用更便宜方式解决了大部分清晰度问题。搁置。复活条件=旧位置擦除(edit-mask)+骨架仍不够锐的场景。EgoWAM 独立解释了失败机制（重建目标把外观逼进网络）。

### 5.4 同构 v2 骨架（flowskelv2）
用户观察：机器人骨架（指线汇聚到前方抓握点，V形）与人手（指线汇聚到后方手腕）拓扑相反；且夹爪开合只靠线宽微弱传达。修复：v2骨架改成人手同构拓扑（从link_6散开到两指尖）+ 开度几何化（3D里按开度把指尖沿指间轴向中点收拢，闭合时并拢）+ 统一3px线宽。验证：张角与开度相关性 r=0.967（俯视）、闭合帧指尖误差 18.6px→0.13px。
结果：flowskelv2 物体误差 0.200/0.238，略逊于 v1 flowskel 0.194/0.229——**同构化对③渲染没有正收益**（渲染外观本就不跨具身共享，符合预判）。但 v2 骨架的价值在②侧兑现（见§6.1，它是②骨架动作实验的基础）。Drive：14b（v2 QC）、15b（人手vs机器人同构对照）、20（②动作骨架）。

### 5.5 治糊杠杆
| 配置 | 俯视 v0 | 结论 |
|---|---|---|
| flowskel base（LAM=1,60轮） | 0.194 | — |
| LPIPS权重×2 / ×4 | 0.205 / 0.228 | 变差（牺牲像素保真凑感知） |
| **训练×2（120轮）** | **0.157** | 大幅变清，PSNR+0.7 |
赢家=延长训练。闪烁噪点若仍在，下一候选=时间一致性损失（detmem线有先例）。

---

## 6. ★human-helps 实验（用户核心命题：让 human 数据帮更多）

诊断（记录在案）：② 的 human-helps 瓶颈不在flow输出（Δflow两域基本同分布，可分性0.53≈随机），而在**动作端输入不同域**（eef绝对坐标星座可分性1.0）。两条打击瓶颈的路线：

### 6.1 ② 用同构骨架作动作表示（drift px，越小越好，机器人稀缺100条）
| 动作表示 | 只机器人 | 加人手 | human增益 |
|---|---|---|---|
| dummy5（旧，绝对星座） | 9.45/9.27 | 6.36/6.90 | −33%/−26% |
| **同构骨架** | 11.08/10.60 | **5.69/5.92** | **−49%/−44%** |
（全量参照 骨架rh 3.43/4.17）
- **同构骨架把human-helps从~30%放大到~46%，且骨架+人手绝对精度也最好**——正面命中"让human帮更多"。
- 是 Pareto（骨架robot-only更差，但有人手就反超）——与LaST-HD预测、与§6.2一致。
- ★归因保留：骨架臂人手侧用了修复后真腕，dummy5用退化中点，增益含数据修复成分，需"dummy5+真腕"对照隔离（TODO）。Drive：19（预测点vs真值4臂overlay）。

### 6.2 latent 残差混训（Task#21移植，latent-MSE，越小越好）
| 臂 | 只机器人 | 加人手 | human增益 |
|---|---|---|---|
| **残差（Δz）** | 0.260 | **0.174** | **−33%** |
| 直接（绝对z） | 0.193 | 0.198 | +3%（不帮） |
- **残差下human帮，直接下不帮**——精确复现"起点跨域不共享、动力学Δz跨域共享"。第二条独立human-helps证据。
- ★注意：此实验自带一个弱解码器（物体误差0.48，远糊于flowskel的0.19），**只作机制层证据，其渲染gif不代表质量**。Drive：18（对比gif，仅供看human-helps方向，画质不作数）。

### 6.3 ★human-helps 穿过好渲染器（用户要求：真渲染44步，不是抽象drift/糊解码器）
把四个②模型（稀缺100条）rollout 44步→同一个flowskel③（120轮最清）渲染，obj/agent-LPIPS（8序列）：
| ② 动作 | 只机器人→③ | 加人手→③ | human增益 |
|---|---|---|---|
| dummy5 | obj 0.306/0.353 | 0.246/0.302 | −20%/−14% |
| **同构骨架** | 0.308/0.362 | **0.217/0.270** | **−30%/−25%** |
- **② 的human-helps真的让最终渲染画面变好**（不只抽象drift）；骨架在渲染层也放大human增益。
- 44帧gif眼检：骨架+人手列罐子位置最贴真值，只机器人列漂走。Drive：22（5列44步真渲染gif，每列叠该臂预测flow红点over真值绿点）、22b（指标表）。
- 用旧物体点（tracks-v2 park）。

---

## 7. 数据体检与修复

三层只读审计（标注层/打包层/消费层），全部python实测数据验证。详见 `CAN_DATA_AUDIT_2026-07-13.md`。
- **CRITICAL（已修）：端到端评估数据泄漏**。②与③的训练/测试划分代码写法不等价（先乱序再过滤 vs 先过滤再乱序），24条测试有22条落在②训练集。修复=SPLIT=okfirst+WM_PT env重训干净②；§4数字已是勘误后。**只影响e2e列，消融表/门控/human-helps不受影响。**
- **人手真手腕缺失（已修）**：打包层从(中心,朝向,开度)合成三点，slot0=指尖中点（丢真腕）。修复=side-car `wrist_sidecar_{robot,human}.npz`（不裁剪真腕+74%有效掩码+朝向矩阵），与另一session bit级一致。使用须wrist_valid过滤。
- 脆弱点（无实测影响）：NaN填充约定不统一；flow_cond的可见性参数是死代码（遮挡从未进③）。

---

## 8. TODO / 未决

1. **物体跟踪点重撒（tracks-v2，park）**：现有48点挤在红罐口一团（俯视凸包覆盖0.33）。SAM2重撒原型/全量：机器人覆盖0.33→0.80/0.84干净成功；人手0.87但溢到托盘/蓝条（约束修复`constrain_to_old_region`已commit未全量重跑）。**目前无实验在用tracks-v2，所有结果用旧点**。恢复步骤在ledger。是否切v2要看它对下游指标的实际收益（需重跑关键实验）。Drive：16（覆盖QC）、17（重撒原型）、21（v2 QC最差最好）。
2. §6.1归因隔离：dummy5+真腕对照。
3. 骨架臂+120轮设默认；闪烁若仍在上时间一致性损失。
4. 候选未做：可见性通道接通、agent/物体分通道、密集网格flow、渲染器辅助预测头、flow相机自运动稳定化（EgoWAM，对头戴人手数据）。

---

## 9. 关键文件与产物索引

- **报告**：本文件、`FLOW_WARP_REPR_LOG.md`（活文档，讨论逐条）、`DUALVIEW_DIT_REPORT.md`、`CAN_DATA_AUDIT_2026-07-13.md`、`SESSION_SUMMARY_2026-07-14_dualview_formalize.md`。
- **③渲染代码**：`exp_scel_dualview_dit_formal.py`（COND=flow/eefsp/eeffilm/flowwarp/flowskel/flowskelv2；MODE=train/eval/gif/e2e）。
- **②代码**：`exp_scel_dualview_wm.py`（ACTION=dummy5/skel，MIX=r/rh，NROB稀缺）。
- **端到端**：`exp_skelact_e2e_render.py`。**latent残差**：`exp_latent_residual_can.py`。
- **IWS外部基线**：`exp_dualview_iws_stage2.py`。**数据修复**：`augment_clips_wrist.py`、`augment_clips_skeleton.py`、`augment_clips_tracksv2.py`。
- **模型**：`dualview_dit_formal/{flow,flowskel,flowskelv2,...}_cv1_s{0,1,2}/dvdit.pt`；干净②`dualview_wm_cleansplit/wm_dual.pt`；稀缺②`dualview_wm_skelact/{action}_{mix}_n100/wm_dual.pt`。
- **Drive**：`iws_evals/2026-07-16_flow表示升级_骨架与warp/`（01-22号，gif+图+表）。
- **数据资产**：`skel_sidecar_{robot,human}.npz`（v1）、`skel_sidecar_robot_v2.npz`（同构）、`wrist_sidecar_*.npz`、`tracks_v2_*.npz`（park）。

---

## 10. 一页纸给论文的故事线

**问题**：用人手演示帮训练机器人世界模型（跨具身）。
**接口选择**：物体运动（flow）作为跨具身共享接口——② 预测它、③ 渲染它。正式化证明 flow 条件显著优于把动作压成向量喂进网络（IWS式），且端到端成立。
**关键机制1（渲染质量+可部署）**：给③加一个从动作几何生成的骨架 agent 条件通道，物体误差降24%、agent误差降28%、长时长零退化，且因为不需要预测所以从replay到部署几乎无损。
**关键机制2（human-helps）**：human 帮不上的瓶颈是动作输入不同域，不是flow输出。把动作表示同构化（骨架）后，human-helps 从30%放大到46%，且穿过好渲染器让最终画面的物体误差降30%。latent残差混训是第二条独立证据（残差跨域共享、直接不共享）。
**诚实边界**：③渲染外观是embodiment-specific，human在渲染层不帮（帮在②）；同构对③无益、对②有益；骨架增益含真腕数据修复成分待隔离；物体跟踪点质量待升级。
