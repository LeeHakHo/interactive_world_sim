# Session 总结（2026-07-12 夜 至 2026-07-14）：论文启发的方法改进 + 渲染质量修复 + 数据价值量化

> 写给项目成员的完整记录。不使用缩写和内部黑话；所有术语在第 0 节解释。
> 所有代码提交在 git 分支 phantom_dynamo（提交号 dd5bdda 到 ab7494b 及之后）；
> 所有动图（gif）已上传 Google Drive，位置见第 7 节；主研究日志为仓库根 `COMBINE_ACTION_LOG.md`。

---

## 0. 术语表（先读这个）

- **世界模型（本项目的"第二级"，代码里叫 ②）**：一个神经网络，输入"过去几帧里物体上 48 个跟踪点的位置 + 机械臂/人手的动作"，输出"这些点接下来会怎么移动"。它预测的是**物体的运动**，不是像素。
- **渲染器（本项目的"第三级"，代码里叫 ③）**：另一个神经网络，输入"第一帧图像 + 世界模型预测出的物体运动"，输出"未来每一帧的完整图像"。
- **动作特征（action featurization）**：把机械臂末端/人手的位置轨迹编码成向量喂给世界模型的方式。不同编码方式是本次实验的核心变量。
- **robot-only / robot+human 训练**：只用机器人演示数据训练，记作 ro；机器人数据加上 1800 条人手演示数据一起训练，记作 rh。**人类数据的帮助 Δ = ro 的误差 − rh 的误差**（正数表示人类数据有帮助）。
- **held-out 测试集**：150 条从未参与训练的机器人片段，所有误差都在它上面测。
- **ADE（平均位移误差）**：预测的物体点位置与真实位置的平均像素距离，越低越好。
- **LPIPS（感知相似度）**：用深度网络特征衡量两张图有多"看起来不一样"，越低越像。obj-LPIPS 只在物体附近 64×64 区域算；full-LPIPS 在整张图上算。
- **PSNR（峰值信噪比）**：像素级图像质量，越高越好。
- **随机种子（seed）**：训练的随机初始化。同一实验跑多个种子取平均，并做配对显著性检验（p 值小于 0.05 才算真差异），防止把随机波动当成结论。
- **双视角**：can 数据集有两个相机（高位俯视 cam_high 和低位侧视 cam_low），世界模型和渲染器同时联合处理两个视角。
- **数据集**：v3 = 早期的方块推动数据集（单视角）；can_dual = 可乐罐操作数据集（双视角，机器人 2700 条 + 人手 1800 条片段）。

---

## 1. 本 session 的任务（用户指令原文要点）

1. 精读新论文（LaST-HD），据此改进现有方法，让"人类数据的帮助"和"最终精度"同时变好；
2. 回应一个审稿人式质疑："你们的贡献是不是只是把动作条件换成了预测物体运动条件，太简单了"；
3. 基础架构定为**双视角联合预测 + DiT（Diffusion-Transformer 风格的注意力网络）**；
4. 有空闲 GPU 就用 sbatch 提交集群任务；
5. 过程中追加的任务：验证"人类数据和机器人数据是否等效"；修复"gif 都很糊"的渲染质量问题；检查人手数据里手腕点的 bug。

---

## 2. 参考论文（读了什么、用了什么）

### 2.1 主要论文：LaST-HD（arXiv 2606.23685，北大，2026-06）
《LaST-HD: Learning Latent Physical Reasoning from Scalable Human Data for Robot Manipulation》
本 session 下载了全文精读（PDF 在仓库根 `2606.23685.pdf`）。与我们相关的核心机制：

- 他们不在动作输入端做人/机对齐，而是训练一个**独立的、以动作为条件的世界模型**（在人手+机器人混合数据上训练，然后**冻结参数**），从它的深层特征里取"物理后果的表示"作为监督目标，用余弦相似度把主模型的内部表示拉向这个目标。
- 他们的关键消融实验（论文图 3b）：用这种"以动作为条件的世界模型特征"做目标得 73% 成功率；换成纯视觉特征（SigLIP）掉到 66%；去掉动作条件掉到 63%；完全不用这种监督掉到 60%。**即：监督目标必须是"以动作为条件的、预测性的、被真实未来验证过的"特征。**
- 他们的立论与我们项目的地基互相印证："推一个苹果产生的物体运动与是谁在推无关"——这正是我们用物体运动（而非像素或动作）作为跨具身接口的理由。

### 2.2 之前引入、本次继续使用的论文
- **OSCAR（arXiv 2606.04463）**：统一骨架动作表示的来源。我们的"skel"动作特征（手腕速度+骨向量）就是按它设计的。
- **DexWM（见仓库根 FLOW_REPR_SURVEY_2026-07-04.md 调研）**："dummy5 虚拟夹爪星座"动作表示的来源（把人手和夹爪都虚拟成同样的五个点）。
- 大规模人类数据预训练的相关工作（LaST-HD、OSCAR、EgoScale 等）在第 5 节的讨论中作为对照参照系。

---

## 3. 尝试一：把 LaST-HD 的机制移植进我们的世界模型（"结合机制"实验）

### 3.1 背景问题
之前的实验已证明一个两难（帕累托前沿）：动作特征里含"绝对位置/接触几何"（world 或 dummy5 编码）→ 最终精度最好，但人手和夹爪长得完全不同，人类数据帮不上；改用"同域"的骨架编码（skel）→ 人类数据帮助大增，但丢了接触信息、最终精度显著变差。**没有一种单一编码能两全。**

### 3.2 我们设计的机制（全部实现并测试，测试驱动开发，单元测试全绿）
在同一个世界模型骨架上，只改动作的用法：
1. **a1（加性残差）**：骨架编码作主体（人+机共训），绝对编码作只属于机器人的"精度残差"（人类样本残差置零），加正则防止残差喧宾夺主。
2. **align（自蒸馏，故意保留的对照组）**：同一网络里两条通路，把绝对通路的特征用均方误差拉向骨架通路的特征（目标随训练移动）。
3. **align_wm（LaST-HD 忠实版，本次新增的主角）**：先在人+机混合数据上训练一个骨架编码的世界模型当**教师**并冻结；学生主通路用纯绝对编码（保精度），训练时加余弦损失把学生内部特征拉向教师特征。教师被真实的未来物体运动监督过（即论文要求的"预测性、被验证过"），并直接复用扫描实验里本来就要训练的骨架基线（同数据、同种子），额外成本约为零。
4. **warm（两阶段课程）**：先用骨架编码人+机共训整个网络，再冻结主干、只用机器人数据训练绝对编码头。

### 3.3 实验规模与结果
在两个数据集上做全量扫描（v3：6 种方法 × 4 档机器人数据量 × 最终 5 个种子；can_dual 双视角：4 种方法 × 3 档 × 5 个种子；slurm 任务号 53157/53158/53212/53213）。

**v3 数据集（held-out 机器人 ADE，像素@224；"rh | Δ" = 最终误差 | 人类数据帮助）：**

| 数据量 N | world 基线 | a1 | align 自蒸馏 | align_wm | warm |
|---|---|---|---|---|---|
| 20 | 9.76 \| +4.88 | 11.99 \| +2.62 | 13.12 \| +2.59 | **10.01 \| +4.83** | 11.43 \| +6.98 |
| 100 | 8.93 \| +0.67 | 9.63 \| +0.80 | 11.48 \| **−1.27** | **8.79 \| +1.29** | 10.97 \| +1.41 |
| 400 | 7.35 \| +2.27 | 8.74 \| +1.78 | 9.15 \| +0.80 | 7.72 \| +1.94 | 9.42 \| +1.34 |

can_dual 双视角上结构相同（详表见 COMBINE_ACTION_LOG.md 第 4.2 节）。

### 3.4 结论（诚实版，含显著性检验）
- **证明了（机制排序，远超随机波动）**：align_wm 是**唯一不牺牲最终精度**的机制——它与精度基线打平，而其它所有机制都用精度换取人类帮助（落后 0.7~2.4 像素）。尤其关键的对照：**自蒸馏版 align 的人类帮助是负的**——同一架构下，唯一区别是"目标是冻结的、被验证过的教师"还是"同网络的移动目标"，直接复现了 LaST-HD 论文消融的机制论。
- **没证明（"突破帕累托前沿"）**：align_wm 相对基线的优势（v3 数据量 100 处最终误差 8.93 对 9.07）在 5 个种子的配对检验下 p=0.61，**属于噪声级**；can_dual 上 p=0.42。3 个种子时曾出现过"突破"的假象，加种子后消失——这正是我们坚持"先量方差再下结论"规矩的价值。
- 论文可写的表述：机制消融（冻结教师 > 自蒸馏 > 输入端改造）+ "帕累托前沿在基线处是硬的"这一加固性负结果。

---

## 4. 尝试二：端到端管线验证（回应"只是换了个条件"的质疑）

### 4.1 设计
四列对照，把链条上每一环的贡献隔离出来：
`真实视频 | 用真实物体运动驱动渲染器（=渲染器的天花板） | 用世界模型预测的物体运动驱动渲染器（=完整管线） | 只给单个末端点动作的朴素渲染器（=基线）`

### 4.2 结果（无数据泄漏的配对模型，can_dual，物体区域感知相似度 obj-LPIPS，越低越好）

| 视角 | 天花板 | 完整管线(align_wm) | 完整管线(dummy5) | 朴素基线 |
|---|---|---|---|---|
| 高位相机 | 0.299 | 0.309 | 0.305 | 0.382 |
| 低位相机 | 0.301 | 0.314 | 0.310 | 0.372 |

世界模型的预测误差只有约 2.6 像素，**几乎不损耗物体运动接口的优势**：完整管线贴住天花板，朴素基线被甩开。这就是对"太简单"质疑的回答：我们的贡献不是"换个条件"，而是"世界模型用潜空间对齐从人类数据学习动力学 → 输出与具身无关的物体运动 → 渲染器消费它 → 整条链在像素层兑现"，每一环都有独立消融支撑。

---

## 5. 尝试三：人类数据与机器人数据是否等效（"汇率"实验）

### 5.1 设计（用户提问触发：rh 比 ro 多用了 1800 条数据，不公平）
把机器人独训的误差曲线补到全量（v3 到 2550 条、can_dual 到 2460 条，各 3-5 个种子；slurm 53243），然后对每个"N 条机器人 + 1800 条人类"的结果在曲线上找等效点：等效于多少条纯机器人数据，除以 1800 得到**汇率 α**。

### 5.2 结果（两个数据集一致）
- **不等效（α 远小于 1），但有实打实的汇率**：α 在机器人数据中等稀缺区最高（v3 数据量 400 处约 0.35~0.47），整体约 0.2~0.5，即**约 3~5 条人类演示 ≈ 1 条机器人演示**。
- **帮助有边界**：机器人数据超过约 1600 条后 α 趋于零；全量时人类数据的边际作用为零（点估计略负但 p=0.33/0.38，**不显著，不能说有害**）。
- **换算成采集成本是正面故事**：LaST-HD 报告人手数据采集比机器人遥操作快 4-5 倍，α≈0.3 × 4-5 倍 ≈ 单位采集时间的价值打平到 1.5 倍。
- 对大规模人类数据预训练论文（OSCAR、LaST-HD、EgoScale 等）的定位：它们赚的是"覆盖"（人类数据覆盖机器人数据到不了的场景/物体），我们量出的是"密化"（同分布内替代）的汇率曲线——两者是同一条曲线的不同区段，互不矛盾；我们的曲线是它们没人给出的量化基础设施。
- 曲线图：`outputs/cross_embodiment_wm/data_equiv/exchange_v3.png` 和 `exchange_dual.png`。

---

## 6. 尝试四：渲染质量修复（用户投诉"gif 都很糊"）

### 6.1 诊断
糊的根源不是物体运动接口、不是 DiT 架构，而是**渲染器没有得到机械臂本身的位置信息**——手臂全靠网络猜测，猜不准就平均化成一团黑影。证据：老的单视角渲染器（detmem，整图感知相似度 0.147）质量好，正是因为它有"机械臂剪影"条件通道；新的双视角渲染器没有继承这个配方。

### 6.2 修复链（每步单变量受控消融，60 轮训练对齐，slurm 53256/53307/53310/53314）

| 版本（逐步叠加） | 高位相机 整图LPIPS | 低位相机 整图LPIPS | 低位 PSNR |
|---|---|---|---|
| 原状（无机械臂条件） | 0.170 | 0.187 | 21.35 |
| + 高位机械臂剪影通道 | 0.134 | 0.165 | 22.35 |
| + 低位机械臂剪影通道（发现现成的低位剪影生成器从未被接入，held-out IoU 0.836） | 0.132 | 0.154 | 23.56 |
| + 网络容量 2.5 倍（对照实验） | 0.129 | 0.155 | 23.42 |
| + 刚体搬运预览通道（把第一帧的罐子像素按预测运动搬到新位置，给网络当草稿） | 0.130 | **0.151** | **23.84** |
| 100 轮定版（双剪影，无搬运） | **0.124** | — | — |
| 100 轮定版（双剪影+搬运） | 训练中（slurm 53314） | | |

**结论**：
- 机械臂剪影条件是治糊的主因（高位 +2.8dB，物体区域 LPIPS 0.281→0.198）；100 轮后整图 LPIPS 0.124，**超过了老单视角渲染器（0.147）且是双视角联合渲染**。
- **网络容量不是瓶颈**：参数扩大 2.5 倍几乎零收益——回答了"DiT 容量够不够"（够，这一档数据量喂不饱更大的网络）。
- 额外发现：低位视角的剪影置零时，它的指标也随高位剪影的加入而改善——**跨视角联合注意力会自发把一个视角的机械臂信息传给另一个视角**，这是"双视角联合"价值的一条新证据。
- 搬运预览对有纹理的罐子有温和改善（低位 0.154→0.151，肉眼可见罐身纹理从"流挂状涂抹"变实体），印证了之前"像素搬运的战场在纹理物体"的预测。
- 低位相机剩余的差距，主要嫌疑是它的物体跟踪点质量（由高位投影播种，噪声大）和低机位本身的遮挡/透视难度，属于数据侧问题，不是渲染器架构问题。
- 用修好的渲染器重跑端到端：预测运动驱动列贴住天花板（整图 LPIPS 0.138 对 0.131），肉眼看夹爪连指头都渲染出来了。

---

## 7. 所有动图（gif）在 Google Drive 的位置

Drive 根目录 `iws_evals/`：

**`iws_evals/2026-07-13_combine_alignwm_e2e/`**（结合机制与端到端）
- `e2e五列对比gif/`：真实视频 | 天花板 | 完整管线(align_wm) | 完整管线(dummy5) | 朴素基线，两个视角 × 6 条片段
- `e2e_alignwm四列/`、`e2e_dummy5四列/`：各自的四列版本
- `Pareto与眼检图/`：帕累托散点图（v3 与 can_dual）、数据汇率曲线两张、末帧对比大图

**`iws_evals/2026-07-13_gmask_agent_cond/`**（渲染治糊）
- `gmask消融三列gif/`：真实视频 | 无机械臂条件（糊版） | 有机械臂剪影（清晰版）
- `锐③端到端4列gif/`：用修好的渲染器重跑的端到端四列
- `100ep质量版/`：100 轮定版的渲染动图

（最终"双剪影+搬运 100 轮"定版和低位视角对比的动图待 slurm 53314 完成后追加上传，同一目录。）

---

## 8. 过程中的事故与额外发现

1. **磁盘写满事故**：/scr2 满（100%）导致一个扫描任务在保存模型时崩溃。处置：从日志抢救回丢失格的数值、清理了可再生的包缓存（14G）应急、给模型保存加了异常保护；后经清理恢复。
2. **人手数据手腕点的 bug**（用户提出，与另一个并行 session 共同定位）：人手演示数据里"手腕"槽位实际被打包成了两个指尖的中点（构造性 bug，出在数据打包脚本，不读上游已有的真实手腕列）。本 session 补充发现：另一 session 用来修复的"真实手腕回填文件"本身有 26% 的帧手腕在画面外（手从画面边缘伸入），这会混杂他们"真手腕没帮助"的负结论。已写入共享记忆供复核。此 bug 不影响本 session 的机制间相对比较（所有编码共用同一份数据），但修好后 align_wm 的教师质量可能提升，值得重跑关键格。
3. **两个 session 的分工**：另一 session 并行在做渲染器的正式化消融（三种条件注入 × 跨视角开关 × 多种子）；本 session 的成果（机械臂剪影通道、跨视角传输发现）已通过共享记忆建议他们并入正式版。他们把朴素基线升级后，"物体运动条件 vs 朴素动作条件"在高位相机上出现了反超（他们的表），这对论文主张有影响，需要两边合看。

---

## 9. 未完成 / 待用户决策

1. 双剪影+搬运的 100 轮定版（slurm 53314）和它的端到端重渲染——自动进行中。
2. 手腕 bug 的数据重生成（另一 session 的管线）；完成后重跑 align_wm 关键格。
3. 低位相机跟踪点质量（投影播种噪声）的数据侧修复——属于另一 session 的双视角数据管线。
4. 渲染器正式化线是否采纳机械臂剪影通道与搬运通道——建议采纳，等两边合并。
