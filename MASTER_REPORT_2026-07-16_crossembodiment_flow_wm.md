# 跨具身物体流世界模型：机制、数据价值与渲染 —— 完整分析报告

> **文档性质**：可下载、可离线分析的自包含总报告。涵盖 2026-07-12 至 07-16 这个 session 的
> 全部工作：问题设定、参考论文（逐篇用法）、每一条实验线（动机 / 候选含放弃的 / 实现 / 完整
> 数字表 / 判决 / 显著性）、方法身份辨析（哪些是我们的、哪些借了 insight、哪里离照搬近）、
> 事故、开放问题、可复现指令、产物索引。所有数字均已对 res.json / summary.txt 核实。
> **作者**：本 session（②机制 / 端到端 / 数据价值 / 渲染质量）。并行 session 负责③正式化。
> **仓库**：`/scr2/yusenluo/interactive_world_sim`，分支 `phantom_dynamo`。
> **配套**：`SESSION_LOG_2026-07-12_combine_render.md`（维护日志）、`COMBINE_ACTION_LOG.md`
> （主研究日志）、`SESSION_SUMMARY_2026-07-14.md`（早期精简版）。

---

# 目录

0. 术语表（先读）
1. 问题设定与项目地基
2. 参考论文（五篇核心 + 逐篇用法）
3. 动作表示：原始数据与三种编码（world / skel / dummy5）
4. 实验线一：动作特征结合机制（align_wm 家族 + stems）
5. 实验线二：端到端管线（②预测运动 → ③渲染）
6. 实验线三：human 数据 vs robot 数据的汇率
7. 实验线四：渲染质量修复（治糊全链）
8. 方法身份辨析（我们的 vs 借的 insight）
9. 结论总表：证成了什么 / 没证成什么
10. 事故与教训
11. 开放问题与下一步候选
12. 可复现指令与产物索引

---

# 0. 术语表（先读这个）

| 术语 | 含义 |
|---|---|
| **②（世界模型）** | 输入"物体 48 个跟踪点历史 + 动作"，输出"这些点接下来怎么动"的网络。预测**物体运动**，不是像素。 |
| **③（渲染器）** | 输入"第一帧图像 + ②预测的物体运动"，输出"未来每帧图像"的网络。 |
| **动作特征 / 编码** | 把机械臂末端 / 人手轨迹编码成向量喂给②的方式。不同编码是核心变量。 |
| **ro / rh** | ro = 只用 N 条机器人数据训练；rh = N 条机器人 + 1800 条人手数据一起训练。 |
| **Δ（human-helps）** | Δ = ro 的误差 − rh 的误差，正数=人类数据有帮助。**它是诊断量，最终看 rh。** |
| **held-out 测试集** | 150 条从未参与训练的机器人片段，所有误差在它上面测。 |
| **ADE** | 平均位移误差：预测点位置与真实位置的平均像素距离，越低越好（②的指标）。 |
| **obj-LPIPS / full-LPIPS** | 感知相似度（深度网络特征距离），越低越像。obj=物体附近 64×64，full=整图（③的指标）。 |
| **PSNR** | 像素级图像质量，越高越好。 |
| **seed / 配对 t 检验** | 训练随机种子；多种子取平均并做配对显著性检验（p<0.05 才算真差异），防噪声当结论。 |
| **双视角** | can 数据集有高位俯视（cam_high）和低位侧视（cam_low）两个相机，②③同时联合处理两视角。 |
| **v3 / can_dual** | v3=方块推动数据集（单视角）；can_dual=可乐罐操作（双视角，机器人 2700 + 人手 1800 片段）。 |

---

# 1. 问题设定与项目地基

**总目标**：用**人类演示数据**帮助训练**机器人**的操作世界模型（cross-embodiment）。

**地基命题（本项目此前已建立，本 session 继续验证）**：人手和机器人夹爪的**观测和动作都是
disjoint（完全可分）的**——像素在 DINOv2 特征空间 linear probe ≈ 1.0 可分，末端绝对位置 probe
≈ 1.0 可分；**但物体的运动是同域的**（Δflow probe ≈ 0.53 ≈ 随机猜测）。直觉："推一个罐子，
无论人推还是夹爪推，罐子的运动是一样的。"

**推论（本项目的核心架构选择）**：让人类数据帮机器人的通道**不走像素、不走动作，走物体运动
（object flow）**。② 在人+机混合数据上共训（人类的物体运动直接是有效监督），③ 只需消费域不变
的流。这条 ②（流预测）→③（渲染）管线就是整个项目的骨架。

**本 session 的五个具体任务**（用户逐次下达）：
1. 精读 LaST-HD，据此改进 ②，让 human-helps 和最终精度同时变好；
2. 回应质疑"你们只是把动作条件换成预测运动条件，太简单"；
3. 基础架构定为双视角联合预测 + DiT；
4. 有空 GPU 就用 sbatch；
5. 追加：验证 human 数据与 robot 数据是否等效；修复"gif 都很糊"；查人手腕点 bug；
   精读 EgoWAM；维护详细文档。

---

# 2. 参考论文（五篇核心 + 逐篇用法）

用户钦定五篇核心论文：**DexWM、OSCAR、IWS、LaST-HD、EgoWAM**。

## 2.1 LaST-HD（arXiv 2606.23685，北大+CUHK+Simplexity，2026-06，PDF 在仓库根）
- **方法**：训练一个独立的、以动作为条件的世界模型（人+机混合，训完**冻结**），从其深层前向
  动力学特征取监督目标，余弦相似度拉主模型（VLA 的推理专家）的内部表示。总损失 = 动作 flow
  matching 损失 + λ×潜空间余弦损失。
- **关键消融（论文图 3b）**：以"动作条件世界模型特征"为目标得 73% 成功率；换纯视觉特征
  （SigLIP）66%；去掉动作条件 63%；完全不用此监督 60%。→ **目标必须是"以动作为条件的、
  预测性的、被真实未来验证过的"特征**。
- **我们的用法**：① 机制移植 → align_wm（见 §4）；② 数据经济学（其"人手采集快 4-5×"用于
  §6 汇率换算）；③ 对照表（其 Mix-HD 68% < 纯机器人 100 条的 73% 本身是一次汇率<1 的测量）。

## 2.2 EgoWAM（arXiv 2607.08436，Georgia Tech / Danfei Xu 组，TRI 资助，2026-07-08，PDF 在仓库根）
- **方法**：HPT 骨干（每具身独立 stem → 共享 trunk）+ 动作头（flow matching）+ **可换插世界预测头**；
  robot 遥操 + Aria 第一视角 human 数据共训；**推理时扔掉世界头**（训练期表示塑形，零部署成本）。
- **核心问题**：什么世界表示最利 human→robot 迁移。三判据：外观抽象 / 表示"效果"非"agent" /
  分离相机自运动。受控对比三目标：像素 VAE latent < DINO 特征 < 相机稳定化 3D flow。
- **结果（3 双臂任务，1800 真机 rollout）**：动作层 BC 共训在行为不对齐时塌穿基线（43→20%），
  3D-flow 世界头稳（75-80%）；DINO 给最强 OOD 泛化（至 4×），3D flow 给最大域内空间精度（20-30%）。
- **我们的用法**：① 主线第三方确认（human 可迁移通道=场景动力学非动作）；② 补做 stems 机制
  （其隐式对齐，见 §4，判决=稀缺区不够）；③ 解释我们 formalize 线的 eefsp 反超（对齐度高时
  强动作基线域内能打）；④ 差异化定位（它不渲染、不量数据价值，我们互补）。

## 2.3 OSCAR（arXiv 2606.04463）
- **用法**：skel（骨架）动作编码的来源（腕速度 + 骨向量）；大规模混训 + warm-start 的代表作，
  §8 讨论"大规模 human 预训练怎么看"时作对照。

## 2.4 DexWM（见 `FLOW_REPR_SURVEY_2026-07-04.md` 调研）
- **用法**：dummy5 虚拟夹爪五点星座的来源（把人手和夹爪都虚拟成同样五个点）。

## 2.5 IWS（Interactive World Sim，本项目自身框架）
- 提供 stage2 的 action-conditioned 渲染注入机制（我们的 eef-FiLM 基线代码级等价于它），
  以及整个 latent VAE / detmem 渲染器谱系。

---

# 3. 动作表示：原始数据与三种编码

**原始数据**：每帧**三个 2D 点**（双视角=每视角三点），外加**未来 24 帧**时间窗：
```
slot 0 = 腕点   （机器人=真实腕关节；人手=当前 bug，实为两指尖中点——见 §10.2）
slot 1 = 指尖/夹爪指 1
slot 2 = 指尖/夹爪指 2
```
所谓"动作编码"= 对这同样三点×24 帧做不同数学变换。三种主要编码：

## 3.1 world（绝对星座）—— 精度锚
每帧 6 维：三点各自减物体质心。`[腕−物心, 指1−物心, 指2−物心] × 24 帧`。
保留接触几何（离物体多远 / 从哪接近 / 碰没碰）→ **预测最准**；但人手三点星座 ≠ 夹爪三点星座
（disjoint）→ 人类数据帮不到它的输入端。

## 3.2 skel（骨架编码，OSCAR）—— 教师用的
每帧 10 维，**全部相对量和速度，无绝对位置**：
```
Δ腕（帧间移动） 2维 | 骨1=指1−腕 2维 | 骨2=指2−腕 2维 | Δ骨1、Δ骨2（开合/转动速度）4维
```
描述"手在怎么动"而非"手在哪"。人手/夹爪都是"腕带两根指骨在动"→ **同域，人类演示能教它**。
代价：丢了离物体多远 → 单用精度差。

## 3.3 dummy5（虚拟夹爪五点，DexWM）—— 双视角线精度锚
先构造五个虚拟点再减物心：`腕、中心c=(指1+指2)/2、c±开合半轴、c+垂直轴 → 5点×2×24帧`。
把人手/夹爪投到同一套五槽位（第3/4点间距=开合度），比原始三点规整，仍保绝对位置 → 精度同 world 级。

## 3.4 各线在用什么
| 线 | 动作表示 |
|---|---|
| v3 机制扫描 | world（锚）/ skel（锚+教师）/ 结合机制 |
| 双视角②（含定版 e2e） | **dummy5**（锚+学生主路径）/ skel（教师） |
| ③渲染器动作侧 | 不吃这些编码——只吃②预测的物体流 + 机械臂剪影 |

---

# 4. 实验线一：动作特征结合机制

## 4.1 问题（帕累托两难）
既有结论：接触几何越丰富（world/dummy5）→ 精度越好但越 disjoint → human-helps 越弱；
越同域（skel）→ human-helps 越大但精度越差。**无单一编码两全。** 目标（spec 判据1）：
找一个机制落到精度锚**左上方**（rh ≤ 锚 且 Δ 明显更大）。

## 4.2 机制族（全部 TDD 实现，单元测试全绿）
脚本：`exp_scel_combine_action.py`（v3）/ `exp_scel_dualview_comb.py`（双视角）。

| 机制 | 定义 | 来源 |
|---|---|---|
| world / dummy5 | 绝对编码（锚，精度上界） | 既有 |
| skel | 骨架编码（锚，迁移上界） | OSCAR |
| **a1** | skel 主体 + 机器人专属绝对残差（human 残差置零 + L2 正则防独吞 + 0.1 小初始化） | spec 主攻 |
| **align** | 同网络双通路自蒸馏（L2 拉绝对通路→skel 通路的 detach，**目标随训练移动**） | spec 原案 = LaST-HD 走样简化，**故意留作对照** |
| **align_wm** ★ | 冻结的混训 skel 世界模型作教师（复用 sweep 中本要训的 skel 锚，同数据同种子），学生纯绝对编码主通路 + 余弦损失拉内部特征向教师 | **LaST-HD 忠实版（本 session 新增）** |
| **warm** | 两阶段：先 skel 混训整网 → 冻结主干只训绝对头 | spec |
| **stems** | 每具身独立动作编码器，**零显式对齐**，共享 trunk + flow 目标隐式对齐 | **EgoWAM 式（本 session 新增）** |
| （未做）aux / adversarial / target-side Δ | — | spec 备选，adversarial 高风险 |

**关键实现决定**：align_wm 教师按臂分开（ro 臂教师只见机器人数据、rh 臂教师见混合）→ Δ 度量
不被教师泄漏污染；评价路径不传 is_h（缺省全机器人、残差全开）→ 评价代码零改动。

## 4.3 教师如何训练（此前反复问，完整版）
教师**就是一个普通的骨架编码运动世界模型**，无任何特殊之处：
1. 数据：与学生同一子集、同种子、同片段索引（rh 臂教师用"N机器人+1800人手"，ro 臂教师只用 N 机器人）；
2. 样本：输入=物体点历史 + skel 动作编码，监督=数据里真实的下一帧物体点位置；
3. 损失：运动离散成 15×15 速度格 → 加权交叉熵；自回归滚 16 步 + scheduled sampling，训 60 轮。
   **无蒸馏、无对齐，就是普通"预测物体怎么动"**；
4. 训完**冻结**。价值 = 其主干中间特征编码了"被真实未来校验过的、人+机共同塑造的前向动力学理解"。

学生训练：`总损失 = 学生预测vs真实的CE + 0.3×(1−余弦(学生主干特征, 教师主干特征.detach()))`；
只更新学生，推理时教师丢掉、学生独立工作（所以 align_wm 与 dummy5 在渲染层打平——只训练信号不同）。

## 4.4 结果（v3，held-out robot ADE px@224，5 seeds 配对）

| 机制 | ro | rh（最终精度） | Δ | 说明 |
|---|---|---|---|---|
| world（锚） | 9.86±0.53 | **9.07±0.34** | +0.79 | 精度基线 |
| skel（锚） | 22.48±3.17 | 12.23±1.30 | +10.26 | Δ 巨大但精度最差 |
| a1 | 10.43±0.40 | 9.63±0.48 | +0.80 | 拿精度换 |
| align 自蒸馏 | 10.20±0.80 | 11.48±0.67 | **−1.27** | **Δ 为负！移动靶有害** |
| **align_wm** | 10.31±0.88 | **8.93±0.37** | +1.39 | 唯一不掉精度 |
| warm | 12.38±0.06 | 10.97±0.60 | +1.41 | 拿精度换 |
| stems | — | 9.53±0.74 | +0.61 | 隐式对齐，输 align_wm |

**双视角（can_dual，drift px@128 两视角平均，N=400，5 seeds）**：

| 机制 | rh | Δ |
|---|---|---|
| dummy5（锚） | 3.44±0.52 | +1.34 |
| skel | 5.31±0.97 | +1.68 |
| align_wm | 3.30±0.38 | +1.36 |
| stems | 3.39±0.30 | +1.39 |

## 4.5 判决（含显著性）
- **机制排序（显著，远超噪声）**：align_wm 全面压制 a1/warm/align/stems（v3 N=100 rh 8.93 vs
  9.53/9.63/10.97/11.48，差距 ≫ 种子波动 ±0.5）。
- **`align` 自蒸馏 Δ 为负（−1.27）**：同架构，唯一区别"目标是冻结 grounded 教师 vs 同网络移动靶"
  → 直接复现 LaST-HD 图 3b 的机制论。
- **`stems`（EgoWAM 隐式对齐）稀缺区不够**：v3 N=100 配对 stems−align_wm = +0.60（5 seed 中 4 个
  同向，**p=0.105**），甚至略差于单共享编码器；充足区（dual N=400）三者打平。→ EgoWAM 隐式对齐
  的成立前提是他们的数据/骨干规模（300+ 演示 + 大骨干 + 海量野外数据），我们规模下不成立。
  **这是我们对 EgoWAM 的增量发现（适用边界）**。
- **突破帕累托（判据1）—— 没证成**：align_wm vs world 锚，v3 N=100 配对差 −0.145±0.52
  （**p=0.61**），dual N=400 配对差 −0.136（5 seed 4 负，**p=0.42**）。3 种子时的"突破★"是噪声，
  5 种子推翻。教训：Δ 大常来自更差的 ro 起点（align_wm ro 10.31 > world 9.86），只看 rh。
- **机制排序定稿**：稀缺区 **显式冻结教师 > 单共享编码器 > 独立编码器隐式对齐 > 输入端改造
  (a1/warm) > 自蒸馏(负)**；充足区全收敛。

---

# 5. 实验线二：端到端管线（②→③）

## 5.1 设计（回应"只是换了个条件太简单"）
四列隔离每一环：`真实 | 真实运动→③(天花板) | ②预测运动→③(完整管线) | 朴素单点动作③(基线)`。

## 5.2 定版结果（warp+双剪影 100ep ③，配对无泄漏 ② ckpt，obj-LPIPS / full-LPIPS）

**H=20（标准）**：
| 视角 | 天花板 | ②align_wm | ②dummy5 | （旧）朴素基线 |
|---|---|---|---|---|
| cam_high | 0.164 / 0.124 | 0.187 / 0.131 | 0.190 / 0.131 | ~0.382（早期 eef 版） |
| cam_low | 0.180 / 0.131 | 0.228 / 0.144 | 0.227 / 0.143 | ~0.372 |

**H=44（长程，回应"步长太短"）**：
| 视角 | 天花板 | ②align_wm | ②dummy5 |
|---|---|---|---|
| cam_high | 0.160 / 0.126 | 0.191 / 0.136 | 0.184 / 0.132 |
| cam_low | 0.190 / 0.140 | 0.223 / 0.150 | 0.222 / 0.149 |

## 5.3 判决
- **②的预测误差（约 2.6px ADE）几乎不损耗接口优势**：完整管线 full-LPIPS 贴住天花板
  （H=20 gap ≤0.013，H=44 gap ≤0.010），**长程零崩溃**，t=47 眼检夹爪罐子依然锐利。
- **对"太简单"的正面回答**：贡献不是"换条件"，而是完整链条 —— ②用潜空间对齐从人类数据学
  动力学（§4 机制消融支撑）→ 输出域不变物体流 → ③经空间对齐注入消费（§7 注入消融支撑）→
  像素层兑现（本节 e2e 支撑）+ 量化数据价值（§6）。
- **两个②机制在像素层打平**（与 §4 的 N=400 统计一致）—— 诚实报，机制差别在②的 ADE 层，
  经③渲染 + VAE 软化后被抹平。
- **泄漏教训**：最早管线验证误用 L48 全量②对 L24 heldout（时间窗重叠），数字偏乐观
  （ADE 2.1 vs 干净 2.6）；正式数字全部换配对 L24-split ckpt。

---

# 6. 实验线三：human 数据 vs robot 数据的汇率

## 6.1 设计（用户质疑 rh 比 ro 多 1800 条数据不公平）
补机器人独训曲线 ro(N) 至全量，对每个 rh(N) 找等效点 ro(N+M)=rh(N)，汇率 α=M/1800。

## 6.2 v3 world 完整曲线（ADE px@224）
| N | ro（纯机器人） | rh（+1800人手） |
|---|---|---|
| 20 | 14.65 | 9.76 |
| 50 | 11.23 | 8.96 |
| 100 | 9.86 | 9.07 |
| 200 | 10.01 | 8.31 |
| 400 | 9.63 | 7.35 |
| 800 | 8.21 | 7.00 |
| 1600 | 6.66 | 6.49 |
| 2550（全量） | 5.66 | 5.93 |

## 6.3 判决（两数据集一致）
- **不等效（α≪1），但有实汇率**：α≈0.2~0.5，峰值在中等稀缺区（v3 N=400 处 0.35~0.47）→
  **约 3~5 条人手 ≈ 1 条机器人**。
- **帮助有边界**：N≥1600 后 α→0；全量时 Δ≈0（v3 rh 5.93 vs ro 5.66，点估计略负但**逐 seed
  方向不一致 p=0.33/0.38，不显著，禁说"有害"**）。这是信息饱和的必然：机器人数据铺满分布后，
  人类那 1800 条不再含新信息。
- **换算采集成本**（LaST-HD 报告人手快 4-5×）：α≈0.3 × 4-5× ≈ **单位采集时间价值打平到 1.5×**。
- **免费单点铁证**：v3 上 rh(100)=9.07 好于 ro(400)=9.63 →"100机器人+1800人手 胜 400机器人"。

## 6.4 与大规模 human 预训练（OSCAR/EgoWAM 等）的定位
它们买"**覆盖**"（human 数据到达机器人到不了的场景/物体/任务；开放世界里机器人永远在曲线左端
=稀缺区=α 最高处）；我们量"**密化**"（同分布内替代）的汇率与衰减。**同一条曲线两段，不矛盾**；
我们的 α(N) 曲线是它们没给出的量化基础设施。未做候选：**填洞实验**（人为留出物体位置空洞，
看 human 填洞的 α 是否 ≫ 密化的 0.3）——把"覆盖派"直觉变成可测定律。

---

# 7. 实验线四：渲染质量修复（治糊全链，收益最大）

## 7.1 诊断
用户记得"can 之前渲染挺好"——查证：好的是老单视角 detmem 渲染器（full-LPIPS 0.147），配方含
**gmask 机械臂剪影条件通道**；新双视角 DiT 渲染器没继承 → 手臂靠猜 → 均值化黑影 = 糊主因。
**不是流接口、不是 DiT 架构的问题。**

## 7.2 修复链（`exp_scel_dualview_gmaskcond.py`，每步单变量受控，全 sbatch）
| 步骤（逐步叠加） | cam_high obj/full | cam_low obj/full | 判决 |
|---|---|---|---|
| 原状（无机械臂条件） | 0.281/0.170 | 0.299/0.187 | 基线（=糊版） |
| +高位剪影（maskgen_caneef） | 0.198/0.134 | 0.254/0.165 | **大胜 +2.8dB**；cam_low 也涨=跨视角注意力自发传输 agent 信息（新发现） |
| +低位剪影（maskgen_caneef_low，**IoU0.836 早训好但从未接过**） | 0.192/0.132 | 0.233/0.154 | cam_low +1.2dB |
| +容量 2.5×（384×8→512×12） | 0.186/0.129 | 0.236/0.155 | **容量判死不是瓶颈**（答"DiT 容量够不够"） |
| +warp 搬运预览（刚体 Umeyama 把首帧罐子像素搬到预测位置作 RGB 草稿） | 0.184/0.130 | 0.222/0.151 | 温和有效；眼检罐子"流挂纹"变实体；印证"warp 战场在纹理物体" |
| 100ep（双剪影，无warp） | 0.170/0.122 | 0.212/0.146 | 超老 detmem（0.147）且双视角 |
| **100ep 定版（双剪影+warp）** | **0.164/0.119** | **0.197/0.137** | **终选**（`final_warp_100ep/dvdit_g.pt`） |

## 7.3 质量物理边界（答"还是有些糊"）
**VAE 编码-解码往返 full-LPIPS 下限 = 0.072（高位）/ 0.078（低位）**（实测）。我们定版 0.119/0.137
中约一半是 16× 压缩 VAE 在 128 分辨率的固有软化，**换 VAE（f8→f4）或 256 分辨率之前不可消除**。
渲染器自身余量还剩 +0.05/+0.06（长程累积等）。

## 7.4 metric 审计（答"物体区域捕捉得准吗"）
发现真缺陷：footprint 凸包未滤不可见点（nan 点被替换到图像中心 → 拽偏 64×64 裁剪框，cam_low 更重）。
修正（`fp_vis`：可见点过滤 + 报检出率）后**全量重评 6 个配置：数字与原值几乎逐位一致、排序不变、
检出率 100%** → 缺陷真实但数值影响可忽略，结论经受审计。修正版已替换所有后续评测。眼检图
`outputs/cross_embodiment_wm/dualview_gmask/metric_audit.png`。

## 7.5 治糊候选清单（试过的 + 放弃的，用户要求记全）
| 候选 | 状态 | 结果/理由 |
|---|---|---|
| agent 剪影条件（双视角） | ✅ 采纳 | 主因，见上表 |
| 容量扩大 2.5× | ✅ 试过判死 | 参数×2.5 ≈ 白给，数据喂不饱 |
| warp-as-condition | ✅ 采纳（温和） | 纹理物体有效，眼检可见 |
| 100ep 长训 | ✅ 采纳 | 稳定小增益 |
| 换 f4 VAE / 256 分辨率 | ⏸ 待用户 | 天花板 0.072→更低，成本大 |
| 扩散采样替代确定性 | ✖ 不做 | detmem 线判过确定性赢；eval 方差大 |
| GAN 对抗损失 | ⏸ 最后手段 | 训练不稳 |
| 低位视角重新裁剪 | ✖ 证据不足 | 物体点已占画面 85%，"浪费像素"假设不成立 |
| 更多 prev 记忆帧 | ⏸ 候选未试 | — |
| 低位 tracks 质量修复（投影播种噪声） | 移交 | 属另一 session 数据管线 |
| human 侧 SAM2 手部剪影通道 | ⏸ 候选未试 | 现 human 样本该通道置零 |

---

# 8. 方法身份辨析（我们的 vs 借的 insight）

用户原则："不能照搬别的 paper，虽然看他们的 insight。" 逐条归属：

| 成分 | 归属 | 说明 |
|---|---|---|
| 物体流作跨具身接口 + 流→渲染完整管线（②→③） | **我们的** | 核心命题，五篇 paper 无一渲染 |
| 双视角联合流预测（DualLWC）、双视角 DiT 渲染器 + 剪影/warp 条件栈 | **我们的** | 架构 |
| 机制地图（显式教师 vs 隐式 vs 输入端 vs 自蒸馏 + 稀缺/充足边界）| **我们的** | 受控实验产出，五篇 paper 无 |
| 数据汇率曲线 α(N) + 衰减边界 | **我们的** | 量化基础设施，五篇 paper 无 |
| dummy5 星座、离散运动类别 | 借（DexWM/AMPLIFY）| 特征工程层，非方法主体 |
| align_wm 的"冻结 grounded 教师"原则 | **离 LaST-HD 最近一处** | 借机制**原则**；实例化/用途/实验设计是我们的 |
| stems 的隐式对齐 | 借（EgoWAM）| 作为对照机制引入，判决=我们的增量（适用边界） |

**align_wm 与 LaST-HD 的实质区别（为何不算照搬）**：
- 他们是 VLA **策略**（输出机器人动作），教师是微调的**视频扩散世界模型**，对齐监督 LLM 推理专家；
- 我们两端都是**运动预测器**（输出物体流不输出动作），教师是我们自己的流世界模型，服务于
  "可渲染的世界模型"而非策略；
- 教师按 ro/rh 臂分开 + 复用 sweep 锚（保证 Δ 度量不掺水、零成本）—— 实验设计是我们的；
- 借的只有一条**机制原则**（对齐目标须独立/冻结/被未来验证），且我们把它做成**受控消融**
  （换自蒸馏翻车、换隐式对齐稀缺区不够）——这个机制地图三篇 paper 都没有。

**paper 的正确写法**：不是"我们提出 align_wm"（太近），而是"**我们提出可渲染的双视角物体流
世界模型，并系统研究人类数据如何进入它 —— 发现潜空间对齐（引 LaST-HD 原则）是唯一不付精度
代价的通道，给出其适用边界与数据经济学**"。align_wm 是"被检验的机制"不是"发明"。

**让 align_wm 本身更有自己东西的两个候选（未做）**：① 对齐目标从"内部特征"换成"预测的物体流
分布本身"（对齐发生在我们独有的接口上）；② 教师用双视角几何一致性约束（利用我们双视角独有结构）。

---

# 9. 结论总表：证成了什么 / 没证成什么

| 命题 | 判定 | 证据 |
|---|---|---|
| 流接口 > 朴素动作条件，且穿过②预测误差在像素层仍成立 | ✅ **证成** | e2e full-LPIPS 0.131 贴天花板 0.124 vs 朴素 0.38；眼检；H=44 长程稳 |
| 机制排序：潜空间对齐 > 输入端改造；冻结 grounded 教师 > 自蒸馏移动靶 > 隐式对齐 | ✅ **证成** | 差距远超噪声 + 干净消融（align 负、stems 稀缺区输） |
| align_wm 吃 human 数据时不损失最终精度（相对其它机制的独有性质） | ✅ **证成** | 唯一 rh 与锚打平的机制 |
| human 数据有可量化的价值汇率 α≈0.2-0.5（3-5 条 human ≈ 1 条 robot）| ✅ **证成** | 两数据集一致的 ro(N) 曲线 |
| 渲染"糊"= agent 无条件化（非架构/接口问题），可修 | ✅ **证成** | 剪影通道 +2.8dB，定版 0.119 超老 detmem 0.147 |
| DiT 容量是渲染瓶颈 | ❌ **证伪** | 容量×2.5 ≈ 零收益 |
| **align_wm 最终精度显著好于普通混训基线（=突破帕累托）** | ❌ **未证成** | 0.14px，p=0.61/0.42，5 种子推翻 3 种子的假象 |
| human 帮助可被机制无损放大 | ❌ **未证成** | 放大 Δ 的机制全部掉精度 |
| EgoWAM 式隐式对齐在我们规模下够用 | ❌ **证伪** | stems 稀缺区输显式教师（增量发现：适用边界） |
| 全量 robot 时 human 数据有害 | ❌ **不成立** | Δ≈0 不显著（p=0.33/0.38），曾口误说"有害"已更正 |

**一句话**：机制地图 + 数据汇率 + 渲染治糊 + 端到端流接口 —— 这四个是扎实的正/负结果；
唯一"突破帕累托"没证成（锚点处前沿是硬的，加固"action gap 靠机制消不掉"）。

---

# 10. 事故与教训

1. **磁盘写满**（07-13）：/scr2 100% → sweep 任务 torch.save 崩。处置：从 slurm log 抢救数值
   （只真丢 1 格）、清 uv 缓存 14G（HF 缓存 84G 绝不能动——离线加载依赖）、torch.save 全包 try/except。
   教训：长跑保存必须容错，大实验前 `df`。
2. **人手腕点 bug**（用户 + 另一 session 发现，本 session 独立核验+补充）：打包脚本
   `gen_flow_render_dataset_caneef.py::human_loader` 不读 parquet 真腕列，slot0 按构造=两指尖中点
   （距中点 0.13px；机器人真腕 24.9px）。**本 session 新发现**：修复用的回填文件 `human_wrist2d.npy`
   有 **26% 帧腕在画面外**（手从画缘伸入）、7.8% 整段出画 → 混杂另一 session"真腕不帮"的负结论，
   复核需滤出画帧。对本 session 结论无影响（各编码共用同一数据，相对比较成立）；修好后 align_wm
   教师质量或提升，值得重跑关键格。眼检图 `data_equiv/wrist_check.png`。
3. **stems 两起工程崩溃**（空 regs stack / _save 无锚 KeyError）：训练值全部从 log 抢救、零损失，
   修复 + 补测试防回归。教训：测试缺口让 bug 溜进集群。
4. **组件版本泄漏**：早期 e2e 误用 L48 全量②对 L24 heldout；正式结果全换配对 ckpt 并声明版本。
5. **gif 列标题不可读**（用户投诉"四列哪个是哪个都不知道"）：曾用截断 ckpt 文件名当列名；
   改为编号+人话（`1 REAL | 2 CEILING | 3 PRED-A | 4 PRED-B`）+ caption 说明；写进 gif 规矩 memory。
6. **口误更正**："全量时 human 有害" → 实为 Δ≈0 不显著。

---

# 11. 开放问题与下一步候选

1. **两 session 合看（重要）**：另一 session 正式化表里升级版朴素基线 eefsp 在高位相机反超 flow
   （0.2284 vs 0.2556，低位 flow 仍赢）；EgoWAM 解释=对齐度高时强动作基线域内能打。paper 应报
   对齐度 + 做不对齐子集，别回避反超。他们的表：`dualview_dit_formal/ablation_table.md`。
2. **腕点数据重生成**（另一 session 管线）后：重跑 align_wm 关键格（教师质量或提升）+ 复核 dummy5w 负结果。
3. **formalize 线采纳**：gmask 双剪影通道 + warp 通道 + 修正版 fp_vis metric（已建议，warp 已被采纳）。
4. **换 f4 VAE / 256 分辨率**的质量跃迁（突破 0.072 感知下限，成本大，待用户）。
5. **数据汇率填洞实验**（覆盖空洞处 α 是否 ≫ 密化 α）——把大规模预训练直觉变可测定律。
6. **align_wm 差异化**（§8 末）：对齐目标换成物体流分布本身 / 双视角几何一致性教师。
7. **human 侧 SAM2 手部剪影通道**（现 human 样本该通道置零，cam_low 想吃满剪影收益需 low 视角 human mask）。
8. **EgoWAM 的 DINO 目标**：若做泛化实验，给②/③加 DINO 特征预测辅助头是现成候选。

---

# 12. 可复现指令与产物索引

## 12.1 环境
- conda `iws`：`/scr/yusenluo/anaconda3/envs/iws/bin/python`（numpy1.26；base 的 numpy2 会崩 cv2）；
- GPU 任务一律 sbatch（snoopy1 单节点 8 卡）；跑前 `squeue` / `nvidia-smi`；HF 离线 `HF_HUB_OFFLINE=1`；
- git：本仓库大量代码未提交是常态，只 add 自己的文件，commit 不加 Co-Authored-By。

## 12.2 关键 ckpt
- **③ 渲染器定版**：`outputs/cross_embodiment_wm/dualview_gmask/final_warp_100ep/dvdit_g.pt`
  （加载需 `GMASK=1 GMASK_LOW=1 WARP=1` + `DualViewDiTG` 类在 `__main__`，见脚本 RENDER_CKPT 模式）；
- **② 配对**：`outputs/cross_embodiment_wm/dualview_comb/extra400_s2/wm_{align_wm,dummy5}_rh_N400.pt`；
- **剪影生成器**：高位 `outputs/flow_wm/maskgen_caneef/maskgen_caneef.pt`（IoU0.79）、
  低位 `outputs/flow_wm/maskgen_caneef_low/maskgen_caneef.pt`（IoU0.836）。

## 12.3 脚本（全部有单元测试 tests/test_*）
| 脚本 | 作用 | 关键 env |
|---|---|---|
| `exp_scel_combine_action.py` | v3 机制 sweep | ANCHORS / COMBINES / N_ROB_LIST / SEEDS / OUT / SMOKE |
| `exp_scel_dualview_comb.py` | 双视角机制 sweep | 同上 |
| `exp_scel_dualview_e2e.py` | 端到端（早期） | WM2 / NSEQ / OUT / COMPARE |
| `exp_scel_dualview_gmaskcond.py` | 渲染消融 + 锐③ e2e + 长程 | GMASK / GMASK_LOW / WARP / DIM_G / DEPTH_G / HORIZON / RENDER_CKPT / E2E_CKPT / E2E_DS / WM2A / WM2B / COMPARE |

## 12.4 数据
- v3：`outputs/flow_render_dataset_v3/clips_{robot,human}.npz`（单视角，机器人 2700）；
- can_dual：`outputs/flow_render_dataset_can_dual/clips_{robot,human_L24,robot_L24}.npz`
  （双视角 tracks/tracks_low/eef/eef_low/vis/vis_low/low_valid，机器人 2700 + 人手 1800，L48/L24 两版）。

## 12.5 结果树（本地）
`outputs/cross_embodiment_wm/{combine_action,dualview_comb,dualview_e2e,dualview_gmask,data_equiv}/`
每目录含 summary.txt / metrics.json / res.json / render_cache / gifs。

## 12.6 Google Drive（★单一完整交付包，用于异地下载分析）
**`iws_evals/2026-07-16_MASTER_交付包/`** —— 本报告引用的全部产物一处齐全（75 文件，216 MiB）：
- `00_报告/` — 本报告 + SESSION_LOG + COMBINE_ACTION_LOG（markdown）
- `01_端到端gif/` — 定版四列端到端 gif，H20（20步）+ H44（44步长程），可读标题
- `02_治糊消融gif/` — 剪影开关对比 + warp 开关对比（两个消融）
- `03_图表/` — 机制Pareto图 / 数据汇率曲线(v3+can) / metric审计 / 腕点bug核验 / gif标题示例
- `04_数字表_summary/` — 所有 summary 汇总 txt + 逐 seed 原始 res.json（供异地重算显著性）
- `05_论文PDF/` — LaST-HD / EgoWAM / OSCAR 全文
- `README_先读这个.md` — 目录结构 + gif 列图例（下载后先读）

**gif 列说明表**：`1 REAL`=真实视频；`2 CEILING`=真实运动→③（渲染器天花板）；
`3 PRED-A`=②align_wm 预测运动→③（完整管线）；`4 PRED-B`=②dummy5 预测运动→③。
Flow 行点：绿=物体真实位置、红=模型预测位置、黄=机械臂动作输入。

（历史散包，已被上面单一包覆盖，可忽略）：`2026-07-15_gmask_agent_cond/`、
`2026-07-13_combine_alignwm_e2e/`、`2026-07-13_gmask_agent_cond/`。

## 12.7 论文 PDF（仓库根）
`2606.23685.pdf`（LaST-HD）、`2607.08436.pdf`（EgoWAM）、`2606.04463v2.pdf`（OSCAR）；
DexWM 见 `FLOW_REPR_SURVEY_2026-07-04.md`。

## 12.8 文档
- 本报告：`MASTER_REPORT_2026-07-16_crossembodiment_flow_wm.md`（下载分析用）
- 维护日志：`SESSION_LOG_2026-07-12_combine_render.md`
- 主研究日志：`COMBINE_ACTION_LOG.md`
- spec：`docs/superpowers/specs/2026-07-12-combine-skel-world-action-design.md`

---

*报告结束。所有数字已对 res.json / summary.txt 核实。如需某条实验线的更细粒度（逐 seed 原始值、
逐帧 gif），见对应结果目录的 res.json / render_cache。*
